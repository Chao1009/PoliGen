# Run 2026-09-03 — status

Started 2026-09-03 by the supervising session, after a 14-agent inventory of the
whole tree (44 open items across six domains). Plan: `PLAN.md` next to this file.
Baseline at start: `a94fd6e`, clean, **363 doctest / 17 203 863 assertions /
1 skipped**, **211 pytest** (35.75 s).

**Where it ended**, re-measured 2026-09-05 after the close-out, on this
machine with `env.sh` sourced: **401 doctest cases / 17 240 286 assertions /
1 skipped / 0 failed** (2 m 30 s), **926 pytest passed / 112 skipped**
(127.6 s; three identical runs gave 127.6 / 128.9 / 129.1 s — the counts are the
invariant, not the clock), the strict citation gate **1205 references / 97 ranges / 7
external / 0 broken / 6 allow-listed** on `PHYSICS_CHANNELS.md` plus **19 /
115 / 0 broken** on `SPIN32_FINITE_GAMMA.md` and **8 external** on
`PYTHIA_BRIDGE.md`, and **101/101** SPDX headers. All nine
`validation/reference/*.json` are **byte-identical to `a94fd6e`** (`git diff
a94fd6e -- validation/reference/` is empty) and the cases that pin the
shipped default against them at **rtol 1e−12** are inside the 401 that
pass, so no default moved in six phases. One page for a reader who wants
the run rather than the phase: `SUMMARY.md` beside this file.

| phase | state | commit | notes | record |
|---|---|---|---|---|
| A b₁ publication ban (item 10, conditions 1–7) | DONE | ac22331 | A1 `MstwSF` over the on-disk PYTHIA grid; A2 gate rerun — **G3b passes at 0.843243 for the MSTW2008 LO nucleon input at CDKS Eq. (21)'s δ-function, and NOT for the shipped `ToyF2` default, which stays at 0.440, outside [0.5, 2]**; A3 the normalisation arbiter (HERMES b₁ᵈ is per nucleon); A5 CD-Bonn — 1.000338, below the reference figure's own digitization error and specific to CD-Bonn *and* MSTW together; A4/A6/A7 the CDKS normalisation applied, the r-default kept, the ban lifted **for the measured configuration only** across the **17 files** that carry it (the measured count: `phase_A_gate_mechanics.md` §4 enumerates them and `git grep -il 'publication ban'` returns the same 17 at `ac22331` and at `HEAD`, re-measured 2026-09-05; PLAN.md A7's "fourteen" is the pre-survey estimate and phase_A_numbers.md's "sixteen" the mid-survey one). Fix round: `--b1-unpol {toy,ct18nlo,mstw}` makes that configuration reachable (it was not — the pipeline hard-wired `ToyF2`), G3a's counting ceiling recorded as a qualified pass. **374 doctest cases / 17 208 032 assertions / 1 skipped, 261 pytest, 1049 refs 0 broken** | `phase_A_numbers.md`, `phase_A_gate_mechanics.md`, `phase_A_cdbonn.md`, `phase_A_miller_normalisation.md` |
| B radiative tail correctness (item 9) | DONE | d3ac125 | **B1** `C0Shape{Ho,VmcFt}` — the ⁶Li C0 shape ships as a band and the published sign of `(1/6)σ^el_T/σ^el_U` at x = 0.10 is withdrawn as a band edge. **B2** the leading-log s-/p-peaks promoted into `src/core/rc.cpp` and `RcTailModel::TPeakPlusLL`; the “< 1 % at Q² ≥ 20 GeV²” acceptance is met **event-weighted** (+0.61 %) and **fails per cell** — 331 of the window's 1356 accepted cells (24.4 %) differ by > 1 %, worst ×6444 at x = 0.79, y = 0.0088, with 318 of them at y < 0.1; per cell it holds only for 0.15 ≤ y ≤ 0.7 (0.55 %), and it is 59 % at y = 0.985. The 2026-09-03 “0.16 % at y ≤ 0.7” claim was a four-point sample read as a window statement and is **withdrawn** (corrected 2026-09-04; T8(d)(i) now gates the low-y corner). **B3** the polarised quasi-elastic tail gets a price tag, not a calculation: `qe_tensor_scale` (default 0.0, shipped npz byte-identical), a **borrowed magnitude and not a derived bound**; measured Δ(ΔA_zz) 0.086 % / 0.0003 % / 0.25 % of the band at x = 0.01 / 0.10 / 0.30. **B4** the tagged `rc_tail ≡ 1` is half a kinematic fact — the elastic half is a veto, the quasi-elastic half is an omission of the same order as the inclusive quasi-elastic dilution; documented exclusion, no new term. **B5** the design record's `ℑ^el_6` had a spurious `η_A`; verified against `polrad2t.tex` and the FORTRAN, corrected in place with a dated box — and the CODE never carried it (Eq. (A.4) at `Q_N ≠ 0` is only on the unimplemented `PolradFull` path), so document-only, no number moved. **B6** the A = 2 → A = 6 transfer of `δ(x)` gets a price: `a_transfer_frac` (default 0.0, output byte-identical), `δ_eff = δ√(1+f²)`; band half-widths on A_zz at x = 0.01, Q² = 5 **1.358472e−04 / 1.518818e−04 / 1.921170e−04** at f = 0 / 0.5 / 1. The **Gakh–Shekhovtsova SHAPE is a recorded rejection**, measured from the paper's own arXiv EPS: δ ≡ (Δσ_RC − Δσ_Born)/Δσ_Born (inferred, corrected 2026-09-04) is singular across the b₁ zero-crossing (−1.691…+7.810 at Q² = 4), not monotone (−0.400 at x = 0.85 vs δ_high = 0.015), has no panel covering x = 0.01 Q² = 5, and is a ratio against *their* Born. `δ_low`'s source re-verified at **0 INSPIRE citations** (record 647050). **377 doctest cases / 17 216 856 assertions / 1 skipped, 268 pytest, 1071 refs 0 broken** (assertions re-measured 2026-09-04: the previously published 17 216 626 predated T8(d)(i)'s 230). | `phase_B_numbers.md` §§B1–B6 |
| C coherent ⁶Li + VMC (items 11, 1) | DONE | adec442 | **C1–C6 all done.** C4/C5 (2026-09-04): ΔB **defined once** at `CoherentScenario::eps_b0` (`delta_b_m`, `slope_at_azimuth`) and the old label "ΔB₀/B" corrected — it was off **by a sign**, not a factor 2; `eps_b0` = −0.08 measured to imply Q_charge(⁶Li) = **−0.9345 fm², 11.42× the measured −0.0818**, the ⁶Li band re-derived as −(0.0070…0.0527), default **kept** (reference gate) with the cost recorded. C5.1 **no η dial** — `quadrupole_for_eta` converter instead. C5.2 the ANL MC errors are **carried** (`VmcRadial::dpsi`/`shifted_by_sigma`/`norm2_error`, `cluster_vmc_mc_sigma`) and measured **negligible** (0.02 % on the tagged tensor observable). C5.3 the ±5 % **propagated**: 5.311 % on N_αd, 7.181 % on the D-wave norm, **2.729 %** on P_D, 0.048 % on `tensor_dilution`. C5.4 the deuteron control reaches **AV18** (opt-in; the flag had been silently ignored), P_D 0.045 → **0.057600**. C5.5 the split is real (+11.61 % / +6.58 %) but the item's "two channels of one run" is **false** (one run builds one channel) and substitution is the **wrong fix** — it overshoots the ab-initio 0.848 by 6.8 %. **C5.5b (2026-09-04): C5.5's own repair shipped a FALSE claim to five sites.** "Nothing inside one run is inconsistent" was wrong — `li6_alpha_channel` set `dis_target = DEUTERON()` unconditionally and the T1 `ClusterBreakup` built its deuteron at P_D = 0.045, so a `--cluster-wave vmc` α-tag run held the VMC α–d overlap and the scenario deuteron together: **+2.069 %** on every polarized tagged-α observable, exact (g₁A is linear in eff_pol; ratio 1.020687209533 at every (x, Q²) measured). Both now follow the flag (`DEUTERON_AV18`, `BreakupOptions::source`) — opt-in path −2.027 %, default bit for bit, `vmc` whole-nucleus reading **0.887076** (measured end to end at 0.886169 ± 0.0011 on 400 k events). `validate()` now also **refuses** `cluster_wave` on `inclusive`/`coherent`, where it is never read — the route by which C5.5's 11.61 % reached a user. Minor: the "`phase_D_numbers.md` §Q3" citation (CONVENTIONS.md, §C5.3) named a section that does not exist — the knob is **Q4**; both citations fixed. **C6 (2026-09-04): the two disagreeing committed copies of the Mäntysaari ask (3 % vs 4 % on the point-radius match, both correct against different reference radii) reconciled into ONE canonical draft**, `mantysaari_collaboration_draft.md`, informed by C2/O5. **O5 REOPENED AND REVERSED 2026-09-04 (second pass): the answer is MARGINAL, not NO.** The first pass priced coherent J/ψ from ONE lepton channel over ONE Q² window (0.1 < Q² < 100 GeV², which `estarlight_li6.md` §1 records as arXiv:2511.05638's *acceptance-study* kinematic range) and got 0.749 σ / 160 fb⁻¹/u. A fresh eSTARlight run of the same build, beams and seed with `MIN_GAMMA_Q2` lowered (§2f, `estarlight_li6_q2_floors()`, T10c) shows **85 % of the coherent J/ψ rate is below Q² = 0.1** — σ 1.773 → **11.971 nb**, \|t\| slope unchanged to 0.4 % — and the μ⁺μ⁻ channel the write-up had *computed and then not applied* adds √2. Together **×13.5 in rate: 2.62 σ at one EIC year, 3 σ at 13 fb⁻¹/u, INSIDE the {1, 10, 100} band.** The old self-check pinned the verdict on the restricted row (`assert lumi_for_3sigma_jpsi > 100.0`, unfailable); it now pins both directions. Also fixed: `EPS_DET_CHANG = 0.1775` and the measured-radius slopes {55.0, 54.8, 54.1} were **retyped** into `o5_a2_reach.py` under a docstring claiming "three in-tree inputs and NOTHING ELSE" — both now live in `coherent.hpp` (`COHERENT_JPSI_EFF_IR8_LI7`, `EstarlightLi6Row::b_rmeas`) and are pinned; the headline is quoted on the **background-immune** ⟨P_zz²⟩ = 0.81 rather than the optimal 0.90; `Optics::lumi_fraction` is now *stated* with its size (0.0781 at 10 × 100, ×0.279 on every S) instead of silently omitted; and the closed-form/not-Good–Walker caveat now rides with the number at `OPEN_ITEMS_SOLUTIONS.md`'s summary row, `PLAN.md`, `configs.py`, `a2_from_geometry` and design G. **What is NOT established** and is stated everywhere the number is: no detection efficiency exists below Q² = 0.1 in any source this tree has seen, and the far-forward working point is unchosen. So request (c) is a request for the polarized J/ψ calculation again — **in photoproduction** — with both gaps inside the ask; requests (a)/(b) unaffected. **Still not sent** — sending is the author's call. **O3 bookkeeping**: still unanswerable in the form asked (no GFMC configurations in this tree), but a genuinely correlated input already committed (`he4.dd`) bounds the SIZE of 4-body correlation on a comparable position-space moment at **+18.2 % (variance) / +8.7 % (rms)** against the uncorrelated-product null (`validation/o3_alpha_correlation_bound.py`) — several-to-twenty percent, not a factor of several, and not a measurement of the split itself. **388 doctest cases / 17 217 290 assertions / 1 skipped / 0 failed, 289 pytest, 1092 refs 0 broken; 0 reference JSONs moved (all seven sub-phases).** *(385/17 217 169/283 before the O5 second pass; 386/17 217 245/287/1088 before the C5.5b pass added T27, the `validate()` refusal case and two pytests — +45 assertions exactly, so no pre-existing assertion moved.)* *(Every `path:line` in the C7–C9 narrative that follows is quoted as it stood at `31ed181`, before the phase-D/E line shifts: `phase_E_numbers.md` §E1.5 measured 260 pre-existing citations as stale by exactly +1 and chose not to re-anchor a record. The two LIVE examples — the `g1_nucleus` carve-out and the sixth exemption — are re-pointed above to today's lines.)* **C7 (2026-09-04): the anchor gate could not fail, and that is why three phases of drift got through.** `validation/check_physics_channels_links.py` accepted any textual occurrence within **±2 lines**, so it reported "0 broken" while **53** `path:line` anchors that pointed EXACTLY at their declaration at `31ed181` pointed at a comment, a blank line, a closing brace or **another symbol's declaration** (13 of those, which read as correct) — 14 off by +1 in `cluster_config.hpp`, 19 off by +2 in `coherent.hpp`, 3 + 1 in `src/core/tagged.cpp`, the rest further. The gate is **strict by default** now: the reference must land on the exact line that DECLARES or DEFINES the name, with one principled carve-out (a use site in a file that does not declare the symbol — `src/core/xsec.cpp:89` `g1_nucleus` is one) and **6 named exemptions, each with its reason** (their drift protection was overstated at the time — see C8); `--fix` re-anchors onto the declaration rather than onto the nearest textual match; `--loose` keeps the old rule for editing a file whose line numbers are in flux. **160 references re-anchored across 27 files** — the 53 regressions plus **107 pre-existing**, most of them sitting on the doc comment one line above their declaration since before this run. `python/tests/test_doc_link_gate.py` (**16 tests**, 37 after C8, 48 after C9) exercises the gate itself, so it is no longer hand-run only. Also: `VmcRadial::shifted_by_sigma`, bound but named by no pytest, gets two. And §C6.2's *"left in place … not deleted"* / *"annotated, not rewritten"* was **false at four sites** — the C6 pass DELETED the ask blockquote and both editor's notes from two dated records; the deletion is kept (it is what "reconcile into ONE canonical draft" means) and the four claims now say so, under a stated rule: a finding is never dropped, a duplicated artefact is replaced by a pointer. **C2 THIRD PASS (2026-09-04): the MARGINAL verdict survives, but the EFFICIENCY CHAIN under it had TWO DEFECTS PULLING OPPOSITE WAYS, and §C2.8's "every assumption, in one list" carried NEITHER.** (1) **ε_det was applied at the wrong beam energy.** `COHERENT_JPSI_EFF_IR8_LI7` = 0.1775 is a ⁷Li number at **18 × 117.9 GeV/u — that nucleus's own TOP energy** (this tree's own row for those beams has ⟨W⟩ = 43.2); the O5 rate sample is ⁶Li at **10 × 99.5**, ⟨W⟩ = 30.2. arXiv:2511.05638 measures exactly that dependence in its §V.B on ³He — **32.23 % / 54.38 % / 99.77 % at 183 / 100 / 41 GeV/u**, verified against the PDF — giving d ln ε/d ln E = **−0.866, −0.681** and hence **×1.122–1.158 UP** for the 117.9 → 99.5 step (ε_det = 0.199–0.206). **It is NOT the paper's own 1.687**: that is their 183 → 100 ratio, a step **3.56× larger in ln E**, which carried whole would give 3.40 σ / 7.8 fb⁻¹/u and would have failed the old `2.0 < S < 3.0` pin. ~~The ⁷Li → ⁶Li substitution is worth a further ×1.16–1.22 at fixed rigidity — the paper's species list is entirely at A/Z × E = 275 GeV/e.~~ **RETRACTED by the C2 FOURTH pass below.** Both levers now live in code (`chang26_he3_energy_scan()`, `chang26_species_efficiency()`, plus `COHERENT_JPSI_EFF_IR8_LI7_E_ION_GEV`/`_W_MEAN_GEV` and `EstarlightLi6Q2Row::w_mean_gev`), where the header used to record only "none at 10 × 99.5" with no direction and no size. (2) **There was NO decay-lepton acceptance in the chain at all** — the paper's number counts scattered *nuclei* and its own p. 4 says the simulation carries no detector efficiency and no reconstructed-distribution acceptance, so σ × BR(ℓℓ) × ε_recoil had no central-detector acceptance or reconstruction efficiency for the pair. Direction **DOWN**. The **geometry is bounded and is not the problem**: at 10 × 99.5 the J/ψ sits at y = −0.39, nearly at rest, so **A_geom = 0.994** on this tree's own `Scenario::eta_max` = 3.5, never below 0.89 anywhere the beams reach. The **per-lepton reconstruction efficiency is UNBOUNDED here** (the 0.95/track drawing the band's low end is the sibling's own labelled stand-in). (3) **They PARTLY CANCEL — ×1.122 against ×0.897 = 1.007** — which is why leaving both out was worse than leaving out either. (4) **A self-contradiction, printed at two of three sites**: "every single conservatism on its own leaves 3 σ inside the band" is false (the de-squeezed optics row is the counterexample), and `o5_a2_reach.py` printed it **three lines below** its own bullet saying so while the Mäntysaari draft repeated it **and omitted the optics row from its ladder**. Fixed at all three; the draft's ladder has the row. **THE RESTATED VERDICT IS A BAND: S = 2.63 … 3.15 σ, 3 σ at 9.1 … 13.0 fb⁻¹/u — inside {1, 10, 100} at both ends, OPEN BELOW** *(the 3.15 top did not survive the fourth pass below; the low edge and "inside at both ends" did)* (a pair acceptance × efficiency of 0.52 costs "above 2 σ"; 0.117 costs "3 σ inside the band"), with **ε_det the dominant unquantified factor** and **`Optics::lumi_fraction` the single correction that on its own restores a NO** (0.73–0.92 σ, 106–167 fb⁻¹/u — at both band ends). The self-check no longer pins a point: it pins the band, both directions, the cancellation, "not 1.687", and the optics exception, and **nine mutations of the claim were checked to make it fail**. **C2 FOURTH PASS (2026-09-04): the band's TOP was resting on an unquantified factor presented as quantified, and three sites still quoted the point.** (A1) *"Every entry is at the same rigidity, so its A-ordering is a species lever on its own"* — seven sites, including `python/bindings.cpp`'s docstring, which the phrase-grep found and the number-grep had missed — is a **non sequitur, RETRACTED**: at fixed R = A E/Z both E/u = R Z/A and p_z = Z R still vary (E/u 118 → 183 GeV/u, Z 1 → 8, p_z 274 → 2192 GeV), and the chain's own d ln ε/d ln E = −0.68 … −0.87 makes the list's ×1.55 spread in E/u worth **×1.35–1.46** — the size of the whole claimed ×1.16–1.22 species gain. The list is a **joint (A, Z, E/u) lever** and the chain never tested the decomposition it asserted. (A2) The one validation was **mis-specified**: the criterion is a safe distance *from the beam*, so p_T,cut = θ·Z·R **carries the charge**, and the test applied ⁷Li's Z = 3 cut to the Z = 2 ³He/⁴He pair. With (2/3)² restored it predicts **1.0967 against the measured 1.0955 — 0.1 %**, not "overstating by 12 %"; a Z-independent p_T,cut instead fits the *light end*'s absolute values (pred/meas 1.003 / 1.034 / 1.047 for ²D / ⁴He / ⁹Be) with ³He's 16 % residual explained by its E/u = 183 (an 18–22 % shortfall on the chain's own scan) — and it is a *local* form, not a description of the list (¹²C off by 32 %, ¹⁶O by ×3.1). **Three readings, nothing picks between them**, and the directional instruction *"Read the low end"* — printed at three sites — is **deleted**. (A3) The **four same-energy entries** the chain never used: ²D, ⁴He, ¹²C, ¹⁶O all sit at 137 GeV/u and ⁶Li's own fixed-rigidity energy is 137.5, so they bracket A = 6 with **no energy step**. The same closed forms there give ε(⁶Li, 137) = **0.176–0.237, ×0.99–1.33 — straddling 1**. **So "the band's TOP just crosses 3 σ" is NOT ESTABLISHED and is withdrawn**: the top is a span, **2.84 … 3.29 σ** (the five forms give 2.84, 2.86, 3.03, 3.04, 3.29). **The band's LOW edge (2.63 σ), MARGINAL, and "3 σ inside {1, 10, 100} at both ends" (8.3 … 13.0 fb⁻¹/u on every form) are unaffected.** The pin is now `significance_hi_min < 3.0 < significance_hi_max`, which **fails if the crossing is ever re-asserted**, and the scalar keys `significance_hi` / `lumi_for_3sigma_lo` / `band_factor_hi` are **gone** so the top cannot be quoted as a point. (B) Three sites still quoted the forbidden point: `a2_from_geometry`'s docstring (2.62 σ / 13 fb⁻¹/u alone, "two further things are UNESTABLISHED" where the omitted one was the unbounded decay-lepton efficiency, and a bare 0.73 σ for the de-squeezed optics), §C6's live paragraph in `OPEN_ITEMS_SOLUTIONS.md`, and the whole-tree sweep for `2.62` / `13 fb` / `3.15` / `9.1 fb`. All carry the band now. **C8 (2026-09-04): the hardened gate was blind to two more citation shapes in the same document, and 20 range citations had gone stale behind it.** C7 fixed `path:line` `name` and left three things unchecked: **93 `path:first-last` RANGES matched no rule at all** (the point regex needs a backtick straight after the digits), **17 citations carry no name** and were checked only for file existence and line range, and **10 citations adjacent to another were SWALLOWED** as the neighbour's "name" and never matched in their own right. Measured on the same criterion C7 used, **20 of the 89 distinct cited blocks were stale** — offsets +1 to +319, only **4** of them with a blank edge to give them away, so bounds-and-edges alone would have found four. The gate now checks ranges for bounds, non-blank edges, an adjacent name declared inside the range (implemented; 0 of 97 supply one, and attaching the prose name instead was measured wrong 9 times in 11), and **R4, a sha256 fingerprint of every cited block** in `validation/physics_channels_ranges.json` (89 entries) that distinguishes a block that **MOVED** (named, and `--fix` re-points it, carrying the fingerprint) from one **EDITED in place** (re-read the row, then `--record-ranges`); unnamed citations must land on a non-blank, non-punctuation line; bare `` `:first-last` `` continuations inherit the path before them. **The C7 claim that an allow-listed anchor "must still carry its name on the exact line, so a shift is still caught" was FALSE** — `mentions()` matched comments and strings, and the key was `(path, name)`, so an exemption also covered every other citation of that name in that file. Exemptions are now keyed on **(path, line, name)** and carry a **pin**, a distinctive substring of the line. Also: a **base class is not a declaration** (`class ToyF2 : public UnpolSF` had been counting as a declaration of `UnpolSF`; `TensorSF` had five "declaration" lines in `sf.hpp`, now two), which drops named citations accepting more than one line from **156 to 120** and those surviving a uniform +2 drift from **36 to 34** *(**WITHDRAWN by C9: the guard's effect on both counts is 0** — it reaches 6 citations, every one of which already accepted more than one line and already survived +2)*. **32 citations corrected (38 occurrences)**: 19 stale ranges re-pointed, 11 blank-edged ranges tightened, 1 range mis-anchored since before `31ed181` re-pointed by hand (`tagged.hpp:255-257` → `:364-375`, the impulse-approximation paragraph), 1 wrong since `6f67d84` (`CONVENTIONS.md:119-121` → `:237-239`), and **2 unnamed citations that sat on blank lines** (`breakup.hpp:153` → `:160`, a phase-C regression; `tests/test_rc.cpp:588` (anchor read 2026-09-15 "LI6_VMC_C0_Q_CUT_FM + eps") → `:495` (anchor read 2026-09-15 "_n, which is"), blank at `31ed181` too). **And the C7 counts 53 / 113 / 166 are marked non-reproducible**: they were measured against the pre-fix uncommitted document, which `--fix` overwrote. The replacements offered for them were **not reproducible either and are withdrawn (C9)** — 111 and 373 came from two different definitions, neither of them a number the gate prints, and the 1071 beside them was the *`31ed181`* gate's count, not today's. **C9 (2026-09-04): the gate is SOUND — a uniform one-line insertion at the top of all 77 cited files is caught on **1075 of 1075** named point citations and **97 of 97** ranges — but its RECORD was not, and two holes the re-measurement exposed are now closed.** The four claims: (1) the "reproducible replacement" counts **111 / 373 / 1071** are withdrawn and replaced by **one** definition (the `broken` count the gate prints, no sidecar, PYTHIA env unset) and **one** recipe that writes nothing into the tree — `31ed181` gate on `31ed181` everything prints **`1071 references checked, 0 broken`** (the finding: it could not fail); today's gate prints **239 broken** on (`31ed181` doc, `31ed181` sources) and **483** on (`31ed181` doc, worktree sources), both decomposed into their point and range halves. (2) The **156 → 120 / 36 → 34** attributed to the base-clause guard is withdrawn: the guard reaches **6 citations**, all of which already accepted more than one line and already survived +2, so its effect on both counts is **0**. (3) The residual "120 of 1075" was over the wrong denominator — **42 citations are not declaration-pinned at all**, and **29 of those accepted more than one line** (widest **14**, `__init__.py:249` `isotope`; then 11, 9, 6, 6, 6), so the honest figure was **149**. New rule **S3** pins an ambiguous use site by a fingerprint of the cited LINE (27 sidecar entries; a lone use site needs none), which takes the +1 drift survivors from 4 to **2** and the +2 from 36 to **34** — all of them now the `class X {` / `virtual ~X()` shape of RESIDUALS 2. (4) The sixth exemption, `pipeline.cpp` `coherent` (`:567` when C9 wrote this, `:774` in the gate's ALLOW table today), was per-citation in the table but not in effect: `pipeline.cpp` declares no such symbol, so the carve-out accepted `:996` and `:1009` (`cfg_.coherent`) **with nothing printed**; S3 closes that too. (5) **5 point citations (6 lines) and 1 range name PYTHIA's own sources** and were silently unchecked; new rule **D** reads them from `$LIPOLGEN_DEPS/../src/pythia8*/src` (or `$LIPOLGEN_DEPS_PREFIX`, the CMake/pip name for the same prefix, or `$LIPOLGEN_PYTHIA_SRC`), fingerprints them, and when no PYTHIA tree is present prints each as `skipped` **by name** rather than passing in silence — all seven anchors re-read against `pythia8317` and correct. **390 doctest cases / 17 217 416 assertions / 1 skipped / 0 failed, 345 pytest, 1094 point citations + 97 ranges + 6 external / 0 broken / 6 allow-listed under the STRICT gate; 0 reference JSONs moved, `COHERENT_JPSI_EFF_IR8_LI7` still the paper's 0.1775 bit for bit.** *(390/17 217 416/334 before C9 — C9 moved no C++ case and no assertion, added 11 pytests to the gate's own suite, and changed no citation count.)* *(390/17 217 385/333 before the C2 fourth pass — it moved no C++ case, added 31 assertions inside T10d and one pytest, and left the citation counts alone.)* *(390/17 217 385/312/1092 before C8 — C8 moved no C++ case and no assertion, added 21 pytests, and raised the checked-citation count by closing the blind spots; 388/17 217 290/307 before the C2 third pass.)* | `phase_C_numbers.md` §§C1–C9 |
| D sweep findings | DONE | de1a040 | **D2** `--unpol-sf {toy,mstw,ct18nlo}` / `--pol-sf {toy,nnpdfpol}` reach the inclusive, tagged and (unpol only) coherent kernels; `--pol-sf` is read only where the fill carries λ_e·P_e ≠ 0, on no channel under tensor-thirds — the cost of `toy` measured (⁶Li σ ×0.80, A_zz ×1.25, A_∥ ×0.83–0.90). **D1** ⁷Li tensor sector: loud zero + meta stamp on every ⁷Li run; the α+t L = 1 convolution is DEFERRED as open item 15 (its sign flips with the unpolarised backend). **D3** the FSI variants are NOT one — 99.50 % of events differ by more than 1 % (\|w_nucleon/w_cluster − 1\| > 0.01), max ×68.5 (ONE stream: tagged-6Li-alpha, 20 000 ev, seed 1234, tensor-thirds at P_z 0.7 / P_zz 0.6 / P_e 0.7, σ_XN = 40 mb, one end of the mandatory 20–40 mb band); retracted and pinned. **D4** PomSet: T0 bit-identical across 14 sets; ⟨n_ch⟩ −2.6 %/+9.9 % over the 12 DPDF fits (−4.7 % over all), kaons ×2.8; set 4's fallback survives at 29.32 %. **D5** the coherent \|t\| ceiling **STAYS 0.2 on the anchor range** (\|t\| ≤ 0.30, knob-independent); the positivity edge — **0.245** at the shipped `eps_b0`, 2.80 at the measured Q — is derived by `t_positivity_edge` as the **contingent second** reason (§11.6, decision row 12). **Provenance**: one `Pipeline::knob_provenance` table (66 rows) drives meta and banner; matrix pytest over **12 (channel, plan) specs** — every knob on the 7 (isotope, channel) combinations under one plan each, plus the plan axis (helicity-flip / transverse-tensor / tensor-flip / pe = 0) on inclusive-⁶Li and tagged-⁷Li-alpha for the 10 plan-sensitive knobs; **547 cells**, 582 collected tests; the coherent `x_max` edge is the largest rate-carrying cell CENTRE. Residues (latent, recorded in phase_D_numbers §D9): the luminosity-mode `optics` row, the gate's Python `#` lines. **400 doctest / 17 240 262 assertions / 1 skipped, 909 passed, 112 skipped, gate 1145 references checked strict, 97 ranges, 7 external, 0 broken**.  D3/D4/D5 all hit one shared defect — the knob was absent from the npz `meta` — now closed with conditional blocks for FSI, coherent and the T2 tier. | `phase_D_numbers.md` §§D1–D9, `phase_D_li7_rank2.md`, `phase_D_sf_injection.md`, `phase_D_small_items.md` |
| E release hygiene | DONE | 7f68339 | **E1** the strict citation gate now covers three documents (PHYSICS_CHANNELS.md 1145 refs/97 ranges; SPIN32_FINITE_GAMMA.md 19/115 — 15 stale ranges re-derived from live source; PYTHIA_BRIDGE.md 8 external), each with its own allow-list, all run by the suite. **E2** `CITATION.cff`, `AUTHORS`, `SPDX-License-Identifier: GPL-3.0-or-later` on 101 files (`validation/check_spdx_headers.py`, gated); **author name is a placeholder — decision row 13**. **E3** `T2_CHAIN.md`'s ePIC gate corrected to PASSED 2026-09-02; matrix row 7's PYTHIA σ clause rewritten to what is demonstrable (sigmaGen() is meaningless under LHAup strategy 3). **E4** `.github/workflows/ci.yml` (4 jobs) + `build_deps.sh` written and exercised locally end to end (deps 337 s at −j8, job 2 bit-for-bit the author's own prefix); `auditwheel repair` run locally; **neither pushed — decision rows 14/15**. NEVER run on GitHub. Disclosures in phase_E_numbers §E4.7 (network use beyond the allowance, an omitted first failed run). **400 doctest / 17 240 262 / 1 skipped, 925 pytest + 112 refused-base skips, gate 1145 strict + 97 ranges + 19/115 + 8 ext, 0 broken; SPDX 101/0** | `phase_E_numbers.md` §§E0–E4 |
| F close-out | DONE | bd775bc | Whole-diff review under five lenses, then **48 confirmed findings applied 2026-09-05**. The two that touch behaviour: `meta["optics"]` / `meta["pot_config"]` (and the CLI banner header) had been reporting a bare "not read on this run" on every channel that HAS a route — where the classifier IS consulted and only the SENSITIVITY is absent — losing the envelope name the `a94fd6e` files carried; the label built in `Pipeline::knob_provenance` now opens "not read on this run at 10x100 high-acceptance (...)", so the name is in the scalar, in the banner and in the row, through the ONE mechanism (no per-key branch in `bindings.cpp`, which now also reads `b1_model`/`b1_unpol` from the same table instead of retyping their ternary). And `--lumi X` alone reaches luminosity mode: the `events` DEFAULT no longer collides with it. Tests: gate condition 3 (CD-Bonn + MSTW, 1.000338) split out as `item 5v` and, with the four `test_mstw_sf.cpp` cases and **seventeen** data-gated cases (8 `test_tagged.cpp`, 5 `test_cluster.cpp`, 3 `test_coherent.cpp`, 1 `test_pipeline.cpp`), moved from in-case `MESSAGE`+`return` (tallied PASSED) to registration-time `doctest::skip` (tallied SKIPPED) — **22 decorators added in this phase**, counted in the diff (`git diff dffe94e -- tests/`), against 25 removed `MESSAGE` guard lines; the "thirteen" this row carried on the day was an undercount of the same set; `test_rc.cpp`'s undocumented 1e-13 widened back to 1e-14. Docs: the D5 dependency, the FSI window and its Σw/Σw_off-vs-`survival()` label, the ⁷Li file counts, the two censuses, the C0-shape band edge, the ban-lift configuration clause, the species-lever non sequitur, the bare 2.62 σ, "the two things", and every stale tally. Then the close-out documents: `OPEN_ITEMS_SOLUTIONS.md` gained a state board (closed / answered-as-band / opt-in-shipped / deferred-with-design / author-decision, each with the deciding number and its window) and a run preamble; `PHYSICS_CHANNELS.md` gained the rows the consistency lens was missing (the leading-log s-/p-peak internals, the CD-Bonn members and constants, `read_fdeut_k`, the route-reach probe, `coherent_rate_x_edge`, `pdfdata_path`, the two Python setters) and lost a stale claim — `deuteron/fdeut.av18` and `li6.adr.fit` are read by C++ now, on three paths and one, and the row still said no C++ path opened them; `README.md` gained a selector table; `SUMMARY.md` is new. **Final tallies, re-measured 2026-09-05 at the end of the close-out: 401 doctest cases / 17 240 286 assertions / 1 skipped / 0 failed (2 m 30 s), 926 pytest passed / 112 skipped (127.6 s), gate 1205 references checked strict / 97 ranges / 7 external / 0 broken / 6 allow-listed (+ 19 refs / 115 ranges SPIN32, 8 external PYTHIA_BRIDGE), SPDX 101/101.** (1172 → 1205 is the close-out's own 33 added citations; the only re-anchoring was the same 5 `pipeline.hpp` citations the `route_reach` comment edit shifted — twice, because the comment was corrected twice — each re-checked by hand onto the line that declares its name.) **Two more instances of the run's recurring defect, found here:** the `RouteReach` header claimed `--optics yr-high-divergence` moves `tagged-d-p` labels "at 60 events and none at 2000" with no seed, which is not reproducible as written — it is phase D's seed-7/seed-11 pair, and at the shipped default seed the channel is not-read at 60, 400 and 2000; and four sites (three of them written by this very close-out, one live in `OPEN_ITEMS_SOLUTIONS.md` §9) read the C0-shape factors 68 and ~260 as the VALUE of σ^el_T/σ^el_U at x = 0.10 when they are its suppression against POLRAD's deuteron +0.064 — the values are +9.357e−04 (`ho`) and −2.442e−04 (`vmc-ft`). Both corrected. Nothing in the decision table was decided: rows 16–25 were ADDED for the decisions the tree calls the author's and the table did not carry. | `AUTHOR_DECISIONS.md` (Part A B1–B26, Part B), `SUMMARY.md` **Residue, recorded (2026-09-05, final gate):** the range sidecar blesses CONTENT at recording time, so a range recorded onto the wrong block stays "0 broken" — three such CONVENTIONS.md ranges were found by reading and re-pointed (:68-69, :357, :468-469); and the dated run records under `run_2026-09-03/` are NOT gated, so their citations into the live documents drift with every line shift — eleven were re-pointed by hand; the rest are dated records and may still be off by phase F's +5/+9 shifts in PHYSICS_CHANNELS.md. |

States: TODO / IN PROGRESS (with what exists on disk) / DONE (with commit hash).

## Author decisions collected for the batch (Phase F)

Filled in as each phase produces the evidence; nothing here is decided silently.

**This table is the ONE registry** (phase F, 2026-09-05). The tree had kept
four — this table's rows 1–15; `OPEN_ITEMS_SOLUTIONS.md` §10's close conditions
4 and 6; `OPEN_ITEMS_SOLUTIONS.md` §15.5's D1–D13 (mirrored as D1–D12 in
`phase_D_li7_rank2.md` §7); and the three "author to confirm" rows (3, 4, 13)
of `OPEN_ITEMS_SOLUTIONS.md`'s top table — and only rows 7, 8, 12, 13 and
14/15 were ever pointed at by a dependent site, so rows 16–25 below were added
in phase F to carry the decisions the tree calls the author's that this table
did not have (row 25 is phase F's own). **Cite decisions by ROW NUMBER, never by position.** Every row is
drafted in full — question, options, evidence, measured cost, and what changes
in the tree once it is answered — in `AUTHOR_DECISIONS.md` beside this file;
the § ids in the last column are that file's sections, and that file carries
the index the other way round (row → section, checked row by row 2026-09-05).
**One section there has no row here and that is deliberate:** §B15, the
promotion of the run's no-op opt-ins, which the run does *not* call an author
decision — it is listed so the batch is complete, and every knob in it moves
nothing at the CLI default whichever way it goes. If it is to be decided it
becomes row 26; this run did not make that call. A cost the run did NOT
measure is written "unpriced" rather than estimated.

| # | decision | phase | evidence | drafted in full at |
|---|---|---|---|---|
| 1 | **`CdksB1` stops halving the digitized CDKS column** — `B1_CDKS_TABLE_TO_PER_NUCLEON` = 1, so `b1_convolution` / `CdksB1` / `--b1-model cdks` **doubled**. This is a *decision applied*, not merely recorded: the evidence is certain. | A (A3 → A4) | CDKS Eq. (10) carries an explicit 1/A, the text under their Eq. (16) says "b₁ is defined by the one per nucleon", f(y) is normalised to one nucleon, F₁ᴺ = (F₁ᵖ+F₁ⁿ)/2 — and the arbiter, HERMES's published b₁ᵈ, is per nucleon by their Eq. (5), confirmed by inverting their Table II in all six bins (mean ratio 0.946 against 0.473 for per-deuteron). `phase_A_miller_normalisation.md` §§2–3. Blast radius measured: two pinned values in `tests/test_sf.cpp` doubled; **no reference JSON moved** (the only b₁ reference is `"b1_model": "miller"`), so the rtol-1e−12 gate is untouched. | `AUTHOR_DECISIONS.md` §B12 |
| 2 | **Miller's ×0.5 STAYS** — `B1_MILLER_TABLE_TO_PER_NUCLEON` = `B1_PER_DEUTERON_TO_PER_NUCLEON` = 0.5. **Author decision: likely, not certain.** Nothing changed; the status quo is kept deliberately and the reasoning is now written down where the constant lives. | A (A3 → A4) | *For per deuteron (keep 0.5):* Eq. (1)'s densities are "in a target hadron", Eq. (5) is a light-cone correlator in the normalised deuteron state, Eq. (6)'s ½ is fully consumed by the quark-spin average of a spinless pion — no 1/A anywhere in Eqs. (1)/(5)/(6)/(20). *For per nucleon (drop it):* his Table I transcribes HERMES's per-nucleon values unrescaled, his Fig. 5 overlays them, and he tunes P₆q to one of them. **Numerical consequence of the other reading: every `MillerB1` number doubles**, including the DEFAULT inclusive tensor rate and `validation/reference/b1_default_li6.json` at rtol 1e−12. What would settle it: Miller himself, or evaluating his Eq. (20) at x = 0.012 (10.5×10⁻² ⇒ per nucleon; 5.25×10⁻² ⇒ per deuteron), which needs a pion PDF set this tree does not have. `phase_A_miller_normalisation.md` §§4, 7, 8. | `AUTHOR_DECISIONS.md` §B2 |
| 3 | **`Li6ConvolutionOptions` keeps `r_sigma_lt`** rather than adopting the gate's `r1998`. **Author decision.** Nothing changed. | A (A6) | The shipped observable is a ratio: the tensor weight is K/D_φ and D_φ's F₁ comes from `InclusiveKernel`'s own `UnpolSF`, whose R is `r_sigma_lt` because nothing sets it — a different R in the numerator would not cancel ((1+r1998)/(1+r_sigma_lt) is 1.088 at x = 0.1). **Measured cost of the choice** on x·b₁: −5.5 % / +24.6 % / +5.4 % / +2.7 % / −1.9 % at x = 0.05 / 0.10 / 0.20 / 0.30 / 0.50, all of it in the two orbital terms (term (1) is bit-identical — it carries an R-free b₁ᵈ table), where it is ×0.54 to ×0.94 because those terms convolve F₁ against a density integrating to zero and so see the *slope* of R. On the gate the same swap is +2.7 % on G3b (0.8432 → 0.8662), so the verdict does not rest on it. **The third option is now IMPLEMENTED (opt-in) and PRICED — 2026-09-06.** `PipelineConfig::r_source` / `--r-source {unset,sigma-lt,r1998}` threads ONE R hook through `default_inclusive_kernel` into `Li6ConvolutionOptions::r_func` **and** `InclusiveKernel::Options::r_func`. `unset`, the default, does not enter the branch — bit for bit by construction, no reference JSON moved; `sigma-lt` installs the shared object and is measured bit-identical (hence `not-read` in `knob_provenance`); `r1998` moves both halves. **At y = 0.5, Q² = 2.5** the weight K/D_φ — and A_zz, which is −(2/3) of it — moves **−3.8357 % / +26.3988 % / +3.3518 %** at x = 0.05 / 0.10 / 0.30 against option (ii)'s **−5.5148 % / +24.6099 % / +2.6629 %**: sharing the hook shifts the answer by 1.7 / 1.8 / 0.7 points and makes it **larger** at x = 0.10, so it does not shrink the shift. The **cos 2φ amplitude separates the two exactly** — Δ carries no R, so (ii) leaves it bit-identical and (iii) moves it purely through D_φ (−8.33 % / −6.73 % / −3.14 %). **(iii)'s shift is y-dependent where (ii)'s is not** (2 ulp): at x = 0.05, Q² = 2.5 it runs −5.4705 % at y = 0.1 to +2.3678 % at y = 0.9, straddling zero, so a (iii) number must carry its y. And it moves the **unpolarised rate** (σ_pb −0.684681 % on a 2000-event ⁶Li run at x_max 0.95), which (ii) cannot. The row therefore becomes *(iii) with which R?* (`AUTHOR_DECISIONS.md` §B7(d); `../run_2026-09-06/phase_A_numbers.md` §A1). | `AUTHOR_DECISIONS.md` §B7 |
| 4 | **The ⁶Li publication ban is LIFTED**; the mandatory ±100 % band is NOT. | A (A7) | Design §5.4's *Escalation* clause reads the peak ratio **after** the checklist; after it the ratio is 0.843243 (AV18 + MSTW + Eq. (21)) and 1.000338 (CD-Bonn + MSTW + Eq. (21)), both inside the factor of 2. Conditions attached and repeated at every site: **the lift is about a configuration, not about a build** — the shipped default `ToyF2` gives **0.440**, outside the [0.5, 2] window, so numbers must be made with `--b1-unpol mstw` to be covered; it is marginal at Eq. (17) (0.520); the CD-Bonn 1.000338 is a residual below the reference's digitization error and not three-digit agreement with CDKS; and the gate is **A = 2** — it tests nothing about the α–d step, so the band, which comes from Q(⁶Li) vs Q_d, stays. `phase_A_numbers.md` §§0, 3, 8. | `AUTHOR_DECISIONS.md` §B4 |
| 5 | **The `DeuteronConvolutionB1` wave-function default stays `kFdeutFile` (AV18)** even though CD-Bonn + MSTW reproduces CDKS's figure on every landmark. Nothing changed. | A (A5, restated here) | Flipping it would move every pinned gate number in layer 3 and checklist item 4 and every published row of `phase_A_numbers.md` §§0–7. It is a one-line change plus a re-pin and belongs to a task that says so. `phase_A_numbers.md` §8.6 item 1. | `AUTHOR_DECISIONS.md` §B17 |
| 6 | **`run_2026-09-02/phase_C_numbers.md` §8.1c/§8.2's `A_zz(Born)` column is wrong by ×3.253983 — REPUBLISHED BESIDE THE ORIGINAL on 2026-09-06 (§8.1c-corr / §8.2-corr / §8.3-corr; `OPEN_ITEMS_SOLUTIONS.md` §9 carries the same pairs).** The originals stay, labelled with what computed them, so neither registry option is foreclosed: (i) leave with the note and (ii) republish are both still open and (ii) now costs one deletion. | B (B3), A4 (2026-09-06) | The column reproduces exactly as `azz(toy_b1(x, q2, f1, B1Mode::Digitized), …)` — the **deuteron** Miller b₁ table — while the shipped `default_inclusive_kernel` has used `Li6B1(MillerB1)` for every spin-1 ion since before the RC commit. Measured at Q² = 5: −1.473482e−03 / −4.975091e−03 / −1.585230e−04 (deuteron table, = the published column to five digits) against **−4.528242e−04 / −1.528923e−03 / −4.871661e−05** (this generator's ⁶Li) at x = 0.01 / 0.10 / 0.30. The `τ`, `w_hi − 1`, `ΔA_zz` and “band half-width 4.4204e−04” numbers inherit it; the `σ^el_T`, `σ^q_U` and `r_U` columns do **not** and reproduce here to every printed digit. `phase_B_numbers.md` §B3.2. **Recomputed 2026-09-06 (A4):** all eight (x, Q²) points × both C0 edges of §8.1c, the five-row §8.2 band with `τ`/`w_hi − 1`/half-width, and the six run-level rows that inherit the column. `published ÷ shipped = 3.253983147` **exactly** on `A_zz` — it is `1/(LI6_B1_RANK2_TRANSFER × LI6_B1_PER_NUCLEON)`, and `azz` is linear in b₁ because the shipped `b2` is `2·x·b₁` (measured as doubles) — but **not** on `ΔA_zz`, which carries an `A_zz`-independent term. `ΔA_zz` changes **sign at x = 0.01 on both C0 edges at all three Q²** (six entries, where the 2026-09-04 note recorded one) and shrinks by ×0.32 / ×0.41 at x = 0.10 / 0.30. Knob spreads: `fq_scale` and `tail_tensor_scale` spread +0.21 % (third significant figure), `fq_scale` unchanged** (they move only `r_T`), `qe_suppression` and `qe_kf_gev` **×0.31**, `c0_shape` **+15 %** — so the budget's order changes at second place once §9's already-corrected `qe_tensor_scale` row is compared on the same footing (it rises from 4th to 2nd). §9's conclusions **1 and 2 do not depend on the column** (re-measured identical) and **3 does and stands** (×767.6 = the 768 on record). One exception recorded: three of eight `vmc-ft` `ΔA_zz` entries re-measure 2.4–6.3 × 10⁻⁵ relative away from the published values, deterministically, cause not established; the `ho` edge is exact. | `AUTHOR_DECISIONS.md` §B11 |
| 7 | **The Gakh–Shekhovtsova δ(x) SHAPE is REJECTED, on the record** — the log-linear interpolation and its two anchors are kept. **Decision made, not an option overlooked**; the digitisation was deliberately not attempted. | B (B6) | Measured from the paper's own arXiv source EPS (`rrc01/rrc1/rrc4/rrc10.eps`, `Q² = 0.1/1/4/10`, each with `/x` plus RC and Born `/y` arrays): δ ≡ (Δσ_RC − Δσ_Born)/Δσ_Born (the RC as a fraction of the **Born**, the base the paper's own sentence names — inferred from its two curves, corrected 2026-09-04 from Δσ_Born/Δσ_RC − 1) is **singular** across the b₁ zero-crossing the RC itself moves (x₀ 0.20118 → 0.18467 at Q² = 4), spanning **−1.691 … +7.810** there and **−0.887 … +4.091** at Q² = 10, so the bounded ansatz `w = 1 ± δτ` would emit negative weights; it is **not monotone** (−0.400 at x = 0.85 against δ_high = 0.015 — 26.7× in magnitude, and T3 asserts monotone plus `==` on both anchors); its four x windows leave **x = 0.01, Q² = 5 uncovered** (and panels a/b do not even overlap); and it is a ratio against **their** Born (ALLM97 F₂, Callan–Gross b₂, their Eq. (60) A_zz), not this generator's b₁. By-product: the **magnitude** taken, "10–30 %", is the low-x end of the **Q² = 0.1 GeV²** panel alone — the only one inside the paper's own x ∼ 10⁻³–10⁻² — quoted at Q² = 5, now flagged. `phase_B_numbers.md` §B6.3, `design_C_tensor_rc.md` Q7. | `AUTHOR_DECISIONS.md` §B14 |
| 8 | **`CoherentScenario::eps_b0` stays −0.08 and the BAND documentation is replaced.** Nothing numerical changed; the decision is recorded with its cost. | C (C4) | The default implies Q_charge(⁶Li) = **−0.9345 fm²**, 11.42× the measured −0.0818 fm² and 1.52× even the α+d model's −0.615, with a₂(±1, 0.3) = **+0.300** — larger in magnitude than the *deuteron's* own digitized −0.28. The honest ⁶Li band at `slope_b` = 50 is **−(0.0070 … 0.0527)**. It is kept because it is pinned at rtol 1e−12 against `validation/reference/coherent.json` and because the number that would replace it is a factor-7.5-wide band whose own 1σ on η spans a sign change. **Cost, stated:** every *generated* coherent tensor number is **11.4×** the measured-quadrupole expectation. **Amended 2026-09-04 (D5, row 12):** the clause "and `COHERENT_T_MAX_DEFAULT` = 0.2 is a consequence of that" is now stated the other way round — the ceiling's reason is the anchor range, which does not depend on this decision, and the positivity edge (0.245 here, 2.80 at the measured quadrupole) is the contingent second reason, derived by `t_positivity_edge`. So this decision does **not** move the ceiling. Flipping it is a one-line change plus a reference dump — the user's call, because it moves published numbers. `phase_C_numbers.md` §C4, T10b. | `AUTHOR_DECISIONS.md` §B5 |
| 9 | **The tagged deuteron CONTROL channel keeps Hulthén at P_D = 0.045**; AV18 is added as `--cluster-wave vmc` (opt-in, and the flag had been *silently ignored* there before). | C (C5.4) | `fdeut.av18`'s u(k), w(k) **are** the p–n relative waves, so the old "there is no VMC d → p+n table" was false. AV18 gives P_D = **0.057600** (+28 %), worth **−2.03 %** / **−1.18 %** on the channel's vector/tensor dilutions; the relative S–D sign does **not** flip. Hulthén is kept because the control's job is to be the Cosyn–Weiss tagged limit of the same analytic family the ⁶Li channel is built from, because it is pinned across `validation/reference/tagged.json`, and because `fdeut.av18` prints no MC errors so the AV18 row carries no band. `phase_C_numbers.md` §C5.4, T25. **Amended 2026-09-06 — the EVIDENCE moved on two of the four legs, no decision taken (`AUTHOR_DECISIONS.md` §B9(c)'s box):** (1) *"the control's job is to be the Cosyn–Weiss tagged limit"* now argues the OTHER way — the rewritten Cosyn–Weiss gate runs on **AV18**, not on Hulthén (`../../benchmarking/07_cw_sign_investigation.md` §8; the Hulthén pair's f₂/f₀ never reaches √2 anywhere on the grid, so it cannot carry CW's landmarks and the old gate's agreement with "k = 0.30 GeV" was a coincidence of β = 0.30). (2) *"it is pinned across `validation/reference/tagged.json`"* is still true but **weaker**: the two spin-1 `model` blocks were deliberately re-pinned on 2026-09-06 from this library, because polligen's own `tagged._amp2_table` carries the same missing `i^L`, so the pin is now a statement about a file this project generates rather than an external port gate. Legs (3) *no MC errors* and (4) *the analytic family* are untouched. And *"the relative S–D sign does not flip"* stays true of the **stored** ψ₂ = +W — but `build_amp2` consumes φ₂ = i²ψ₂ = **−W** and applied no phase at all until that date, which is the bug that was fixed. The two percentages do not move: **−2.026832 % / −1.182144 %** re-measured (−2.026820 % / −1.182136 % before) on dilutions that re-measure **0.932496109312** / **0.913595978560** (vector) and **0.959488878164** / **0.948146339359** (tensor), all four re-pinned in T25 at rtol 1e−6. **The row stays open.** | `AUTHOR_DECISIONS.md` §B9 |
| 10 | **`LI6_CLUSTER_POLARIZATION` stays 0.811228 and the `--cluster-wave vmc` drift is DOCUMENTED, not closed.** | C (C5.5) | The drift is real — **+11.61 %** vector, **+6.58 %** rank-2 — and on the Hulthén default the "same wave function seen in two experiments" claim is true to **1.22e−5**. But substitution is the wrong fix: the self-consistent reading 0.905427 sits **6.8 % above** the ab-initio six-body VMC **0.848** where the shipped 0.811228 is 4.3 % below it, and with the AV18 deuteron too 0.887076 is still 4.6 % above. The cluster **product** carries the error, not the choice of P_D — `P_D_LI6` = 0.0867 was never an α–d D-state probability but a fitted effective depolarisation. **Cost:** a tagged row and an inclusive row of one programme differ by 11.61 %, and **no inclusive ⁶Li polarization may be quoted without the band 0.81 … 0.91**. The alternative — an opt-in `PipelineConfig` override — is priced in §C5.5 and would move every inclusive g₁ by +11.6 % *away* from 0.848. **Amended 2026-09-04 (§C5.5b):** the clause "nothing in the code can refuse the combination (they are different runs)" was wrong twice over. `validate()` now **refuses** `cluster_wave` on `inclusive`/`coherent`, where it is never read — accepting it unread was the route by which this 11.61 % reached a user. And "nothing *inside* one run is inconsistent" was **false**: see row 11. `phase_C_numbers.md` §C5.5, T26. **Amended 2026-09-06 — one measured residual moved and no leg of the decision did:** the "same wave function seen in two experiments" claim now holds to **7.84e−6**, not 1.22e−5 — the tagged `vector_dilution()` re-measures **0.8699431789** against the closed form 0.869950 (6.82e−6 absolute) after the tagged S–D interference sign fix (`../../benchmarking/07_cw_sign_investigation.md`), which is angle-integrated and therefore moves only the 96-cell midpoint-quadrature residual of `∫Θ₀Θ₂ dc = 0`. The **+11.61 % / +6.58 %** drift this row turns on is unchanged: re-measured 0.116126 (0.116131 pre-fix) and 0.065762, both inside T26's 1e−4 pins. `AUTHOR_DECISIONS.md` §B6(c)'s box. | `AUTHOR_DECISIONS.md` §B6 |
| 11 | **The EMBEDDED deuteron follows `--cluster-wave`, and the flag is REFUSED where it is never read.** | C (C5.5b) | §C5.5's repair shipped a new claim to five sites — *"a single run builds exactly ONE channel, so nothing inside one run is inconsistent"* — and it was **false**. `li6_alpha_channel` set `dis_target = DEUTERON()` **unconditionally** and the T1 `ClusterBreakup` built its deuteron at `P_D_DEUTERON` = 0.045, so a `--cluster-wave vmc` α-tag run used the ANL VMC AV18+UX α–d overlap for the RELATIVE motion and the scenario Hulthén deuteron for the EMBEDDED one: **+2.069 %** on every polarized tagged-α observable (0.9325 against the AV18 deuteron's 0.9136001620 — exact, g₁A is linear in the effective polarization; measured on `InclusiveKernel` g₁ and A_∥ at four (x, Q²), ratio 1.020687209533 at every point). §C5.4's own rationale forbids it (*switching only the control would make the control and the channel it controls two different wave-function families*). **Decision: follow, not refuse and not document.** `DEUTERON_AV18()` (tagged.hpp) and `BreakupOptions::source` (breakup.hpp) both take the flag, so it means ONE deuteron everywhere it is read; `vector_dilution_of` (beams.hpp) is now the one home of 1 − 1.5 P_D. Opt-in path **−2.027 %**; Hulthén default **bit for bit** (0 reference JSONs, 0 pre-existing assertions moved); a `vmc` run's whole-nucleus reading is **0.887076** = `li6_cluster_polarization(VMC_P_D_LI6, deuteron_av18_p_d())`, one Hamiltonian end to end and §C5.5's own third row — measured end to end at 0.886169 ± 0.0011 on 400 k events. ⁷Li deliberately does **not** move (no AV18 A = 3 wave function exists here), stated in code. **Secondary defect, also fixed:** `validate()` accepted `cluster_wave` on `Inclusive` and `CoherentLi6`, where it is never read — the mechanism by which §C5.5's 11.61 % reached a user; refused now, as `cluster_vmc_mc_sigma` (0.02 %) already was. `phase_C_numbers.md` §C5.5b, **T27**. | `AUTHOR_DECISIONS.md` §B10 |
| 12 | **The coherent \|t\| ceiling `COHERENT_T_MAX_DEFAULT` STAYS 0.2, and the reason it is written down changes from positivity to the ANCHOR RANGE.** Nothing numerical changed; no reference gate moved. **Decision applied**, and it is stated here because it *reverses a dependency*: the ceiling used to be documented as a consequence of row 8's `eps_b0`, and it no longer is. | D (D5) | The two reasons the header gave were presented as agreeing; they agree only at the shipped `eps_b0`. The positivity edge in closed form is (1 − sign(A)C)/\|A\| = 2(1/\|P_zz\| − amp)/(\|eps_b0\|·B), i.e. **0.2450** at the shipped −0.08 (P_zz = −2), 0.3719 at the α+d model Q, 1.1462 at GFMC and **2.80** at the measured Q (those three are arithmetic on ROUNDED `eps_b0` = −0.0527/−0.0171/−0.0070; on the derived −0.0526846/−0.0171207/−0.0070024 they are 0.3720/1.1448/**2.7990** — quote 2.80, never 2.8000) — 9.3× outside `mantysaari_a2_deuteron()`'s own \|t\| ≤ 0.30. So a positivity-derived ceiling would license extrapolating a linear-in-\|t\| fit ten times past its data. The anchor range does not move with any knob, which is why it is now primary. **The ceiling is not a rate question either way:** measured on 200 000 generated events at the defaults (seed 99, `tensor-thirds` at P_z = 0.7 / P_zz = 0.6, 4 threads), ⟨\|t\|⟩ = 0.019963, max \|t\| = 0.197575, and `sample_t` renormalizes on [0, t_max], so the truncation redistributes exp(−B t_max) = **4.5e−5** of the rate rather than losing any. `positivity_margin` / `check_positivity` are unchanged and still throw — they are the guard, not the derivation. **What still waits on row 8:** un-truncating the azimuthal modulation (sampling φ from Σ_m p_m e^{−ΔB_m\|t\|cos 2φ} rather than from 1 + c₂ cos 2φ). That is the physically right form and it moves every pinned coherent azimuth, so it is filed as the follow-on to the `eps_b0` decision. `OPEN_ITEMS_SOLUTIONS.md` §11.6, `phase_D_numbers.md` §D5. | `AUTHOR_DECISIONS.md` §B16 |
| 13 | **NEEDS THE AUTHOR'S NAME.** `CITATION.cff` and `AUTHORS` were created (E2) with the literal placeholder `<AUTHOR NAME — to be filled by the author>` in the one `authors:`/copyright slot each carries; no name was guessed. No copyright line was added to any source file — only the license identifier (`SPDX-License-Identifier: GPL-3.0-or-later`, no holder). The git identity on this machine is a project name (`LiPolGen`) and the configured email is not a name, so neither could stand in. | E (E2) | `CITATION.cff` (`version` read from `CMakeLists.txt`'s `project(LiPolGen VERSION 0.1.0 ...)`, `repository-code` from `git remote -v` — `git@github.com:Chao1009/PoliGen.git` → `https://github.com/Chao1009/PoliGen`), `AUTHORS`, `README.md`'s license section (now points at both), `python/tests/test_release_metadata.py` (keeps `CITATION.cff`'s version and the two placeholders in sync with the tree so a future fill-in cannot update one file and miss the other), `phase_E_numbers.md` §E2. | `AUTHOR_DECISIONS.md` §B22 |
| 14 | **PUSH `.github/workflows/ci.yml` (and `.github/scripts/build_deps.sh`), or not — and the tree makes this INSEPARABLE from pushing the run.** Both files exist on disk and **have never been pushed, and the workflow has never been run by GitHub** — no Actions run exists for this repository (unverifiable from here: no network). But `ci.yml` is committed inside `7f68339`, it declares `on: push: branches: [master]` (plus `pull_request`, `workflow_dispatch`), and `master` is **10 commits ahead of `origin/master` = `a94fd6e`** (measured 2026-09-05), so the author's routine push of this run **activates the never-parsed workflow on GitHub's runners at push time**. The three options are therefore: **(i)** push the run as it stands → the 4-job workflow runs on that push; **(ii)** add one commit before pushing that `git rm -r .github` or narrows `on:` to `workflow_dispatch`; **(iii)** do not push. The decision is the author's, because it is a change to their public repository. Restated 2026-09-05; drafted with its evidence as `AUTHOR_DECISIONS.md` §B23. | E (E4) | **What was exercised locally, 2026-09-05, Ubuntu 22.04.5 / glibc 2.35 / gcc 11.4.0 / 8 cores:** `build_deps.sh` was run end to end into a scratch prefix — **337.28 s** (HepMC3 57.7 s, LHAPDF 49.6 s, the three PDF sets ≈12 s, PYTHIA ≈218 s; **more than half of it Python bindings nothing in this tree imports**) — from six upstream URLs each probed for HTTP 200 that day. **The obvious PYTHIA URL is dead**: `pythia.org/download/pythia83/pythia8317.tgz` 404s, as does that whole path for 8310–8318; the script uses `pythia.org/releases/pythia83/…`, so a workflow written from memory would have failed on its first, most expensive job. The repository was then copied into the workflow's own layout (`actions/checkout path: LiPolGen`, `deps/` its sibling) with that prefix, `source env.sh` needing **no CI-specific patching**, and job 2 was run command by command: configure 3.08 s, build 95.93 s / 0 warnings, **C++ 400 cases / 17 240 262 assertions / 1 skipped / 0 failed — bit-for-bit the author's own prefix**, pytest **923 passed / 114 skipped / 0 failed** (923/114 not the tree's own 925/112 of that day because the copy has no `.git` and no sibling `PolarizedLithiumSim`), both gates clean. That whole line was measured at `7f68339`; phase F then added one doctest case and one pytest, so the same job on the current tree would be expected to read **925 / 113** on GitHub, where `actions/checkout` supplies the `.git` and the sibling is still absent. Cache: **431 MiB on disk, 113.5 MiB compressed**, one entry, keyed on the three versions + the three set names. **What is NOT established, and is stated in the file's own header:** `act`/docker/podman are not installed here, so **nothing parsed the file as GitHub parses it** — cache round-trip, the `ubuntu-22.04` image's apt set, real 4-core timings (extrapolated 8–12 min cold, 9–11 min for the test job) and workflow-expression validity are all unverified. Risk was minimised where possible: `${{ env.* }}` survives in four places only. The workflow contains **no upload step of any kind** — not even `actions/upload-artifact` — so pushing it is not by itself an act of distribution. `phase_E_numbers.md` §E4.2–E4.5, `docs/PACKAGING.md` §1. | `AUTHOR_DECISIONS.md` §B23 |
| 15 | **PUBLISH an `auditwheel`-repaired wheel, or not — and if so, decide what to do about the DATA trees and the build machine's paths baked into it.** Nothing was uploaded anywhere; the wheel was built, repaired, verified and thrown away. | E (E4) | `auditwheel` had never been run on this tree. Measured 2026-09-05 with auditwheel 6.8.2 + patchelf 0.19.1 from PyPI in a throwaway venv: `pip wheel . --no-deps` **87.77 s → 1 779 767 B**; `auditwheel repair` **5.35 s → 7 232 971 B** (**4.06×**) under the tag **`manylinux_2_35_x86_64`** (glibc 2.35; a wider tag needs a build inside an older manylinux image), vendoring **`libpythia8` 14.0 MB (GPL-2.0-or-later)**, **`libLHAPDF` 1.24 MB (GPL-3.0)** and **`libHepMC3.so.4` 1.18 MB (GPL-3.0)** into `lipolgen.libs/` and rewriting four RPATHs to `$ORIGIN:$ORIGIN/../lipolgen.libs`. **Portability, measured with the whole deps prefix replaced by an empty tmpfs in a `bwrap` namespace** (not merely with the variables unset): `import lipolgen` with all three tiers `True`, the pure-C++ generator and the wheel's own vendored VMC tables all **work**, `<x> = 0.027023337580082865` bit-identical to the unmasked run; `LhapdfSF` **fails** (`Couldn't find required lhapdf.conf system config file`) and `--hadronize` **fails** (`PYTHIA Error in Settings::init: settings file <build-machine path>/Index.xml not found`), because PYTHIA's `xmldoc`/`pdfdata` and LHAPDF's set store are **not in the wheel** and their compiled-in defaults are this machine's absolute paths. With those two trees present at a *different* path and `PYTHIA8DATA`/`LHAPDF_DATA_PATH`/`LIPOLGEN_PYTHIA8_PDFDATA` exported, everything works and the T2 HepMC3 output is **byte-identical** (sha256 `68ed6f06…e405ac`). **Two things to decide with the publish, not after it:** (i) shipping the data trees would mean a ~300 MiB wheel and raises a separate redistribution question for the LHAPDF grids — not done, not recommended blind; (ii) **the builder's home directory survives as a literal string** in the artefact — `lipolgen/libLiPolGenCore.so` keeps `RUNPATH $ORIGIN:/home/cpeng/Projects/polli/deps/install/lib` (inert, auditwheel never touches a library that needs nothing from the prefix) and four more `…/deps/install/…` strings sit in the vendored `.so`s. **Licence: a statement, not a change.** Vendoring those three makes the wheel a combined work whose union is GPL-3.0-or-later — already this project's licence — so the publisher ships the *combination* under it: corresponding source for all of it, no added restrictions, same terms onward. `phase_E_numbers.md` §E4.6–E4.8, `docs/PACKAGING.md` §2–3, `docs/open_items/engineering.md` §C. | `AUTHOR_DECISIONS.md` §B24 |
| 16 | **The α–d source default stays `FitRescaled`.** Author decision, named as one by the tree (`phase_C_numbers.md` §C3.5 "an author decision", `docs/USAGE.md` §9 "by **author decision**", `OPEN_ITEMS_SOLUTIONS.md` §11 O2) and never carried here. Nothing changed. | C (C3.5) | Options and measured cost in `AUTHOR_DECISIONS.md` **§B8** | `AUTHOR_DECISIONS.md` §B8 |
| 17 | **The RC band's low-x anchor** — `RC_DELTA_LOW_X` = 0.30 at `RC_X_LOW` = 0.01, and what it is anchored to. `rc.hpp` says in so many words that "moving the anchor is the author's call". Nothing changed; the alternatives are **now PRICED** (2026-09-06) rather than merely named. | B (B6) → priced 2026-09-06 | The 0.30 is Gakh–Shekhovtsova's panel value **carried upward in x**, and **0.113 is the value that panel actually READS at x = 0.00966, the x nearest the 0.01 anchor** (0.266 at its own bottom, x = 0.00226) — so the shipped anchor errs **wide by ×2.65**, the safe direction for a half-width and the wrong one for a quoted precision. **Band re-evaluated at δ_low = 0.30 / 0.266 / 0.113** (⁶Li, config 1, inclusive, `--plan tensor-thirds --pzz 0.6`, Q² = 5, P_zz = +1, this generator's own ⁶Li b₁ — **not** §9's ×3.253983 deuteron-b₁ column). Half-width on `A_zz`: **1.358472e−04 / 1.204512e−04 / 5.116913e−05** at x = 0.010, **1.459067e−04 / 1.308566e−04 / 6.313127e−05** at 0.063, **9.680016e−05 / 8.798804e−05 / 4.833349e−05** at 0.100, **1.920691e−05 on all three** at 0.160. The anchor is carried in full only at x ≤ 0.01 (×0.8867 / ×0.3767), **compressed** between the anchors (×0.8969 / ×0.4327 at 0.063; ×0.9090 / ×0.4993 at 0.100) and **identically zero** at x ≥ 0.16, where the E12-13-011 anchor pins it — so §B13(c)'s "≈ ×0.89 / ≈ ×0.38" is exact at the anchor and overstates the reduction everywhere else, and the whole decision lives in x < 0.16. **The ordering of the RC budget survives all three**: the band still leads the whole tail at x = 0.01 by ×768 / ×681 / ×289. It moves **2 of 51 columns and 2 of 59 `meta` keys**, and — measured — **not** the tagged band's clipped-event fraction: the **same** 124 of 20 000 ⁶Li (0.62 %, seed 1) and 520 of 20 000 ⁷Li (2.6 %, seed 11) clip at all three, because `clamp_tau` clips \|τ\| and δ never enters it; what the anchor sets there is the clipped events' width, exactly 1 ∓ δ_low = [0.700, 1.300] / [0.734, 1.266] / [0.887, 1.113]. **The HIGH anchor has no alternative to run** — the record names none: 0.015 is E12-13-011's 1.5 % from the **unpublished proposal only**, and the only alternative any document states is "cite it by page or drop the anchor", which is not a value. **No default moved, no reference JSON moved** (none carries an `rc_*` field), and no existing assertion breaks — the gates on the anchor are symbolic. Pinned by `python/tests/test_rc_low_x_anchor.py` (5 tests). `AUTHOR_DECISIONS.md` **§B13(d)**; `../run_2026-09-06/phase_A_numbers.md` **§A2** | `AUTHOR_DECISIONS.md` §B13 |
| 18 | **G3a's counting window** — the ceiling recorded as a qualified pass (`OPEN_ITEMS_SOLUTIONS.md` §10, "the design's and the author's call"), **and the FLOOR change this run APPLIED after seeing the answer**: the counting window went `[0.02, 1.0]` → `(0, 1.0]` and the scan floor 0.01 → 0.001 in `tests/test_b1_nuclear.cpp`, two margin pins (`z[0] > 0.02`, `z[0] − 0.02 < 0.005`) deleted with no replacement. *Applied — confirm or revert.* | A (A7) | It was motivated by a row that FAILED the old clause (CT18NLO's first zero at 0.0098, pinned at `tests/test_b1_nuclear.cpp` as `c.z0 < 0.01`); argument at `phase_A_numbers.md` §§315–336 and `run_2026-09-02/design_D_b1_li6.md` §5.4's amendment; `REQUIRE(z.size() == 2)` and the ±0.08 / ±0.10 position checks are unchanged and every other landmark is re-pinned at its previous tolerance; T17 pins the third crossing the ceiling excludes. `AUTHOR_DECISIONS.md` **§B4** | `AUTHOR_DECISIONS.md` §B4 |
| 19 | **`PDF:PomSet` = 11 (PomHISASD) is REFUSED** by the T2 bridge — a behaviour change this run applied, in the table for the first time. *Applied — confirm or revert.* | D (D4) | It needs `setXPom`, which the bridge never calls, and 100.00 % of its events take the e_q² fallback; `src/pythia/pythia_bridge.cpp`. `AUTHOR_DECISIONS.md` **§B19** | `AUTHOR_DECISIONS.md` §B19 |
| 20 | **The thirteen ⁷Li rank-2 decisions of `OPEN_ITEMS_SOLUTIONS.md` §15.5 (D1–D13)**, whose own section title is "The author decisions this needs" and none of which was in this table. Two of them are not bookkeeping: **§15.5 D2** decides whether the shipped `toy` default is inside the b₁ ban lift at all (it is not — 0.440, outside [0.5, 2]), and **§15.5 D13** — honouring `--pzz` on `helicity-flip` — **would move a shipped fill** and is now **BUILT OPT-IN AND PRICED** (2026-09-06, `--pzz-mode {ladder,typed}`, default `ladder` = today bit for bit; `../run_2026-09-06/phase_A_numbers.md` §A3). Measured at the standard configuration with `--pzz 0.5` against the ladder: **on ⁷Li it moves no observable** — the rank-2 sector is identically zero and both fills honour `--pz`, so σ agrees to **1 ulp** (1.970 × 10⁻¹⁶ relative) and A_∥ to 6.2 × 10⁻¹⁴ σ_stat — while still moving **0.1060 %** of a 100 000-event sample into other (x, Q²) cells; on ⁶Li (J = 1, rank-2 live) σ moves **−0.0023527 %** and A_∥ **−4.27 × 10⁻⁶ σ_stat**; and on both the **recorded alignment** moves (T 0.4 → 0.5, P_zz 0.409403 → 0.5), which is the divisor of every tensor estimator (**−20 %** / **−18.1195 %** on δ(A_zz) and δ(cos 2φ) at fixed N). `--pzz 0.6` at `--pz 0.7` is REFUSED at J = 3/2 with the edge named (0.26 ≤ T ≤ 0.58), never clamped, and ACCEPTED at J = 1 (σ −0.0049496 %). | D (D1) | `AUTHOR_DECISIONS.md` **§B3** (D2), **§B18** (D13), **§B20** (the remaining eleven); `phase_D_li7_rank2.md` §7 | `AUTHOR_DECISIONS.md` §B3, B18, B20 |
| 21 | **Send the Mäntysaari-group letter — all of it, (a)/(b) only, or wait.** The draft says at its own top that this is "the author's decision alone"; nothing has been sent. Never in this table. | C (C6) | `mantysaari_collaboration_draft.md`; `AUTHOR_DECISIONS.md` **§B25** | `AUTHOR_DECISIONS.md` §B25 |
| 22 | **`TENSOR_LL_SIGN` = −1 — carried forward, confirm only.** `constants.hpp` records it as an author decision already taken (2026-08-29, plans/08 D1) while `OPEN_ITEMS_SOLUTIONS.md` top-table row 3 and `docs/surveys/needs_survey.md` still say "author to confirm". Nothing in this run moved it. | pre-run, built on by A–D | `AUTHOR_DECISIONS.md` **§B1**; the `TENSOR_LL_SIGN = +1.0` quoted in `needs_survey.md` is the SIBLING `fastsim` tree's, not this one's | `AUTHOR_DECISIONS.md` §B1 |
| 23 | **The ⁶Li effective polarization, `LI6_CLUSTER_POLARIZATION` = 0.811228 vs the naive 1/3** — `OPEN_ITEMS_SOLUTIONS.md` top-table row 4's "author to confirm", which row 10 of this table builds on without carrying it. | pre-run, built on by C (C5.5) | `AUTHOR_DECISIONS.md` **§B6** | `AUTHOR_DECISIONS.md` §B6 |
| 24 | **The licence, `GPL-3.0-or-later` — carried forward, confirm only.** `OPEN_ITEMS_SOLUTIONS.md` top-table row 13's "author to confirm"; E2 built on it and **stamped the identifier onto 101 files**. | pre-run, applied by E (E2) | `AUTHOR_DECISIONS.md` **§B21**; `validation/check_spdx_headers.py` | `AUTHOR_DECISIONS.md` §B21 |
| 25 | **The MSTW rows that are still tallied as PASSED when the grid is absent** — T16's `--b1-unpol mstw` reach row and T17's third-crossing row keep an in-case `if (!mstw_grid_present()) { MESSAGE(...) }` because each sits in a case whose shipped-default rows must still run. Phase F split the two rows the lift RESTS on into registration-time-skipped cases of their own (T1v already was one; `item 5v` — gate condition 3 — and the four `test_mstw_sf.cpp` cases now are), and `docs/USAGE.md` now states exactly which rows are which. Split T16/T17 too, or leave them? | F | `AUTHOR_DECISIONS.md` **§B26**; `tests/test_b1_nuclear.cpp` T16/T17 | `AUTHOR_DECISIONS.md` §B26 |
