# Run 2026-09-06 — status

Follow-on to the 2026-09-03 run. Plan: `PLAN.md`. Baseline at start: `5af0427`,
clean, 401 doctest / 17 240 286 / 1 skipped, 926 pytest + 112, gate 1206/96/7/0.

| phase | state | commit | notes |
|---|---|---|---|
| A price rows 3/17/20; regenerate the ⁶Li tables | DONE | cdd8591 | **A4 (2026-09-06), part 1 — the ⁶Li tables are REGENERATED, as a BAND** (`phase_A_li6_tables.md`). `run_2026-09-02/phase_D_numbers.md`'s tables were made with the default `ToyF2` and `OPEN_ITEMS_SOLUTIONS.md` §10 had said since 2026-09-03 that they *"must be REGENERATED under `--b1-unpol mstw` before they are quoted as physics"*. They were rerun — **same observables, same (x, Q²) points, same options, same object** (`Li6ConvolutionB1` directly, which is what made them) — over `--b1-unpol {toy, ct18nlo, mstw}` × `--unpol-sf {toy, ct18nlo, mstw}`, and the `toy` column reproduces `phase_D_numbers.md` to every printed digit, which is what makes the other two a band and not a different measurement. **Which axis moves what is measured, not assumed:** `--b1-unpol` is the deuteron F₁ *inside* CDKS Eq. (22) and moves **terms (2d) and (2α) and nothing else** — terms (1) and (3), the densities, the T14 remainders and the `w_αd = 0` row are **bit-identical** across it, because they convolve b₁ᵈ; `--unpol-sf` is the kernel's own F₁/F₂ and is **exactly flat on x·b₁** (`Li6ConvolutionB1::b1` ignores its `f1` argument; verified bit-identical through the pipeline at all five x for both non-toy `b1_unpol` values), moving instead the **denominator** of A_zz — which spans a factor **2.23** over the seven legal cells at x = 0.10, Q² = 2.5, y = 0.5. **The 3 × 3 matrix is 7 cells, not 9**: `--b1-unpol toy` under a non-toy `--unpol-sf` is refused **by name** by `validate()`, because `meta["b1_unpol"]` would then record "toy" for something else. **THE QUOTABLE BAND** on x·b₁ at Q² = 2.5 is **[+1.574759e−6, +3.650716e−6] / [−6.212120e−6, −2.796612e−6] / [−2.803165e−5, −2.120522e−5] / [−5.324901e−5, −4.173248e−5] / [+4.226952e−5, +5.411263e−5]** at x = 0.05 / 0.10 / 0.20 / 0.30 / 0.50 — spread **×2.318 / ×2.221 / ×1.322 / ×1.276 / ×1.280** — and the **mandatory ±100 % A > 2 band rides on top of every row of it**, `{0, 1, 2} × b₁`, from Q(⁶Li) = −0.0818(17) fm² against Q_d = +0.2859(3) fm² and **not** from the A = 2 gate. **At x = 0.05 and 0.10 the two bands do not contain one another** (the backend spread exceeds ×2), so `{0, 1, 2}` on one backend does not bracket the others there; at x = 0.20/0.30/0.50 it does. **Two published readings DO NOT SURVIVE and are withdrawn as readings, flagged at every site that printed them**: the orbital sector's *"opposite sign to term (1) over the whole window"* (true on `toy` at three of five x, **false at every x on `ct18nlo`**, four of five on `mstw`) and *"w = 2 nearly zeroes b₁ at x = 0.10"* (toy-only; on the two real fits w = 2 moves b₁(0.10) **away** from zero by +24 % / +9 %). What survives, measured on all three backends: the four terms summing to `b1` (≤ 3.1e−16), the band's exact linearity, (2α)/(2d) = 0.477–0.512 wherever readable, ±5 % `norm_target` and ×1.2203 Q4 exactly, T14 ≤ 6e−5; SD/DD **widens** 11.50–11.57 → **11.21–11.70**, `finite_q_delta` **−1.1 % to +8.6 %**, `∫b₁ dx` becomes **[+1.387049e−4, +1.458804e−4]** (5.17 %, the tightest quantity on the axis). Sites rewritten to the band: `OPEN_ITEMS_SOLUTIONS.md` §10, `USAGE.md` §2a, `PHYSICS_CHANNELS.md`, `run_2026-09-02/phase_D_numbers.md` and `design_D_b1_li6.md` §5.4. **No default moved, no option was added** (`--b1-unpol` and `--unpol-sf` both already existed and are both already registered in `knob_provenance`) and **no reference JSON moved**. Gated by `python/tests/test_li6_unpol_band.py` (12 tests) pinning the x = 0.30 row of the band on all three backends at rtol 1e-12 plus the two structural facts. <br><br>**A4 (2026-09-06), part 2 — registry row 6 / §B11: the `A_zz(Born)` column is RECOMPUTED and PUBLISHED BESIDE THE ORIGINAL, which takes no decision away.** `run_2026-09-02/phase_C_numbers.md` §8.1c/§8.2's column is ×3.253983 too large — it reproduces `azz(toy_b1(…, B1Mode::Digitized), …)`, the **deuteron** Miller table, where the shipped kernel uses `Li6B1(MillerB1)`. The originals **stay**, labelled with what computed them, and the recomputation sits next to each as **§8.1c-corr** (all eight (x, Q²) points × both C0 edges), **§8.2-corr** (the five-row band with `τ`, `w_hi − 1` and the half-width) and **§8.3-corr** (the six run-level rows that inherit it); `OPEN_ITEMS_SOLUTIONS.md` §9 carries the same pairs. Registry options (i) *leave* and (ii) *republish* are therefore **both still open**, and (ii) now costs one deletion. **The factor is exact and named**: `published ÷ shipped = 3.253983147` = `1/(LI6_B1_RANK2_TRANSFER × LI6_B1_PER_NUCLEON)` = `1/(0.921947 × 2/6)`, exact because the shipped `b2` is the Callan–Gross `2·x·b₁` (measured as doubles) so `azz` is linear in b₁ — and **`ΔA_zz` does not divide by it**, carrying an `A_zz`-independent term. **Four findings the note did not have:** (1) `ΔA_zz` changes **sign at x = 0.01 on both C0 edges at all three Q²** — six entries, where 2026-09-04 recorded one — and at x = 0.10/0.30 the sign survives while the magnitude falls ×0.32/×0.41; (2) which knob **spreads** move follows one rule — a knob that leaves `r_U` alone has an `A_zz`-independent spread, so `fq_scale` (8.040830e−07) and `tail_tensor_scale` spread +0.21 % (third significant figure), `fq_scale` unchanged while `qe_suppression` 1.680e−07 → **5.167e−08** and `qe_kf_gev` 1.878e−07 → **5.776e−08** shrink ×0.31 and `c0_shape` 2.877e−08 → **3.312e−08** *widens* 15 %; (3) the RC budget's **order changes at second place** once §9's already-corrected `qe_tensor_scale` (1.162e−07) is compared on the same footing as the four that were not — it rises from fourth to second because the three below it shrank; (4) §9's headline conclusions **1** (σ^el_T's sign change in x) and **2** (the σ^q_U/σ^el_U ladder) **do not depend on the column** and re-measure identical to every printed digit, while **3** (*"the tail is not the leading RC systematic"*) does and **stands** — ×767.6 = the 768 on record — and §8.3's *"dominant … by a factor 300"*, which reproduces as neither of its two well-defined ratios (549.7 / 201.6 on its own column), is restated as **168.9×** and **61.9×**. One exception recorded rather than smoothed: three of the eight `vmc-ft` `ΔA_zz` entries re-measure **2.4–6.3 × 10⁻⁵ relative** away from their published values (fifth significant figure), deterministically in this tree, **cause not established**; every `ho` row — which carries all the conclusions — is exact. Row 6's section (d) and `run_2026-09-03/STATUS.md` row 6 both record *republished beside the original on 2026-09-06*. **No source file changed in either half of A4.** End state re-measured: **404 doctest cases / 17 240 384 assertions / 1 skipped / 0 failed**, **1004 pytest passed / 127 skipped** (992 + the 12 new), gate **1220 strict / 95 ranges / 7 external / 0 broken / 6 allow-listed** (six `USAGE.md` ranges re-anchored with `--fix` after the §2a rewrite moved them; no `--record-ranges`, no cited block edited), SPDX **101/101**, `git diff --stat validation/reference/` **empty**. <br><br>**A2 (2026-09-06): registry row 17 — the RC band's low-x anchor — is PRICED; nothing was decided and no option was added** (`--rc-delta-low-x` already existed). The 0.30 at `RC_X_LOW` = 0.01 is Gakh–Shekhovtsova's panel value **carried upward in x**, and **0.113 is the value that panel actually READS at x = 0.00966, the x nearest the anchor** (0.266 at its own bottom, x = 0.00226), so the shipped anchor errs **wide by ×2.65** — the safe direction for a half-width, the wrong one for a quoted precision. **The band was re-run at δ_low = 0.30 / 0.266 / 0.113** (⁶Li, config 1, inclusive, `--plan tensor-thirds --pzz 0.6`, Q² = 5, P_zz = +1, this generator's own ⁶Li b₁ — **not** §9's ×3.253983 deuteron-b₁ column, now flagged in place at both sites that print it). Half-width on `A_zz`: **1.358472e−04 / 1.204512e−04 / 5.116913e−05** at x = 0.010, **1.459067e−04 / 1.308566e−04 / 6.313127e−05** at 0.063, **9.680016e−05 / 8.798804e−05 / 4.833349e−05** at 0.100, **1.920691e−05 on all three** at 0.160. The anchor is carried in full only at x ≤ 0.01 (×0.8867 / ×0.3767), **compressed** between the anchors (×0.8969 / ×0.4327 at 0.063; ×0.9090 / ×0.4993 at 0.100) and **identically zero** at x ≥ 0.16 — so the registry's own *≈ ×0.89 / ≈ ×0.38* estimate is exact at the anchor and overstates the reduction everywhere else, and the whole decision lives below x = 0.16. **The RC budget's ordering survives all three** (the band leads the whole tail at x = 0.01 by ×768 / ×681 / ×289) and **the band's peak stays at x = 0.063** on every anchor, sharpening as it falls. It moves **2 of 51 columns and 2 of 59 `meta` keys** and — measured, not assumed — **not the tagged band's clipped-event fraction**: the **same** 124 of 20 000 ⁶Li tagged-α events (0.62 %, seed 1) and 520 of 20 000 ⁷Li (2.6 %, seed 11) clip at all three, because `RcModel::clamp_tau` clips \ **Post-verification fixes (2026-09-06):** the withdrawn w_hi−1 column at PHYSICS_CHANNELS.md:381 replaced and flagged; 18→15 moved columns (byte comparison); the `--r-source` banner clause no longer claims a rate move at `sigma-lt` (not read); 74/568→76/592; two range citations the sidecar had re-blessed on the wrong block re-pointed by CONTENT (USAGE §7 checks-for-free, §8 command-line generators) — the residue class Phase D closes. **Final: 404 doctest / 17 240 384 / 1 skipped, 1004 passed, 127 skipped, gate 1220 strict + 95 ranges + 7 ext, 0 broken; SPDX 101/101; no reference JSON moved.** |τ\| against `band_tau_max` and δ never enters it; what the anchor sets there is the clipped events' width, exactly 1 ∓ δ_low = **[0.700, 1.300] / [0.734, 1.266] / [0.887, 1.113]**. **The HIGH anchor has no alternative to run and the record names none** — 0.015 is E12-13-011's 1.5 % from the **unpublished proposal only** (the published companion arXiv:2506.04506 has no radiative-correction discussion at all), and the only alternative any document states is an *action*, "cite it by page or **drop** the anchor", not a value. Bookkeeping recorded on the way: §9's published **0.62 %** is seed 1 and the **top** of a ten-seed scan (0.4850–0.6200 %, mean 0.5280 %, sd 0.0411); the ⁷Li 2.6 % is seed 11 and seed 1 there gives 2.51 %. **No default moved, no reference JSON moved** (none carries an `rc_*` field), no citation added and no pre-existing assertion touched. Gated by `python/tests/test_rc_low_x_anchor.py` (5 tests). End state re-measured: **404 doctest cases / 17 240 384 assertions / 1 skipped / 0 failed**, **992 pytest passed / 127 skipped**, gate **1220 strict / 95 ranges / 7 external / 0 broken / 6 allow-listed** (+19/115 SPIN32, +8 external PYTHIA_BRIDGE), SPDX **101/101**. <br><br>**A3 (2026-09-06): registry row 20 / §15.5 D13 is priced, and its option (iii) is implemented opt-in.** `--pzz-mode {ladder,typed}` (Python `make_plan(pzz_mode=...)`, `PZZ_MODES`) names the two branches of `HelicityFlipOptions::use_explicit_pzz`; `ladder` is the DEFAULT and is the branch that was already there, so it is bit for bit by construction (checked as identity of doubles and as byte equality of every generated column against the pre-flag call). `typed` honours the typed `--pzz` and **REFUSES** a value outside the plan's domain with the edge named — never clamps; `spin1_populations` was taught to name its own edges (3\|P_z\|−2 ≤ P_zz ≤ 1) beside `spin32_populations`' (1.8\|P_z\|−1 ≤ T ≤ 1−0.6\|P_z\|), and the CLI turns either into a `SystemExit` instead of a traceback. **PRICED** at the standard configuration (inclusive, config 1, 100 000 events, seed 20260713, `--pz 0.7 --pe 0.7`, `--pzz 0.5` — inside both domains): **on ⁷Li, the isotope the row is about, NO observable moves** — the rank-2 sector is identically zero and both fills honour `--pz`, so σ agrees to **1 ulp** (1.970 × 10⁻¹⁶ relative), σ[apar−] exactly, and A_∥ to **6.2 × 10⁻¹⁴ σ_stat** — while **0.1060 % of the events (106 of 100 000) land in a different (x, Q²) cell** on last-bit arithmetic, so the file is not byte-reproducible across the modes. On **⁶Li** (J = 1, rank-2 live) σ moves **−0.0023527 %** and A_∥ **−4.27 × 10⁻⁶ σ_stat** (a 1σ A_∥ shift would need N ≈ 5.5 × 10¹⁵); the size is the **b₁ model's, not the window's** — `x_max` 0.95 changes it in the 8th figure while `li6-convolution` gives −0.00010719 % and `cdks` −0.00006617 %, 22–36× below `miller`. What moves on both is the **recorded alignment** (T 0.4 → 0.5, P_zz 0.409403 → 0.5), the divisor of every tensor estimator: **−20 % / −18.1195 %** on δ(A_zz) and δ(cos 2φ) at fixed N. The registry's own line `--pzz 0.6` at `--pz 0.7` is **refused at J = 3/2** (0.26 ≤ T ≤ 0.58, outside by 0.02) and **accepted at J = 1** (σ −0.0049496 %). Registered in `knob_provenance` as a MEASURED row (it rebuilds the other mode's fill and counts differing populations), and `--pzz` itself now flips from `not read by plan helicity-flip at --pzz-mode ladder` to `read` at `typed`. Gated by `tests/test_bookkeeping.cpp` + `tests/test_spin.cpp` (2 cases, 40 assertions), `python/tests/test_pzz_mode.py` (24 tests) and two `test_knob_provenance.py` matrix variants (24 cells). **No default moved and no reference JSON moved** (`git diff --stat validation/reference/` empty; `bookkeeping.json` is the only one carrying `helicity-flip` fills — five of them, four from the ladder branch — and all nine are byte-identical). End state re-measured: **404 doctest cases / 17 240 384 assertions / 1 skipped / 0 failed**, **987 pytest passed / 127 skipped**, gate **1220 strict / 95 ranges / 7 external / 0 broken / 6 allow-listed** (+19/115 SPIN32, +8 external PYTHIA_BRIDGE), SPDX **101/101**. <br><br>**A1 (2026-09-06): registry row 3's option (iii) is implemented opt-in and priced.** `PipelineConfig::r_source` / `--r-source {unset,sigma-lt,r1998}` (`RSource`) threads ONE R into `Li6ConvolutionOptions::r_func` **and** `InclusiveKernel::Options::r_func`, so the tensor weight K/D_φ has one R in its numerator and its denominator. `unset` (the default) does not enter the branch — bit for bit by construction; `sigma-lt` installs the shared object and is MEASURED bit-identical (162 values across 3 x × 2 Q² × 3 y, and σ_pb / every per-category cross section / every column of a 2000-event run), which is why its `knob_provenance` row is `not-read`; `r1998` moves both halves. **Priced against (i) and (ii) at y = 0.5, Q² = 2.5:** K/D_φ (and A_zz = −(2/3) of it) **−3.8357 % / +26.3988 % / +3.3518 %** at x = 0.05 / 0.10 / 0.30 against (ii)'s **−5.5148 % / +24.6099 % / +2.6629 %** — a gap of 1.7 / 1.8 / 0.7 points, and (iii) is **larger** at x = 0.10, so sharing the hook does not shrink the shift. The **cos 2φ amplitude separates the options exactly**: Δ carries no R, so (ii) leaves it bit-identical and (iii) moves it purely through D_φ, −8.33 % / −6.73 % / −3.14 %. **(iii)'s shift is y-dependent and (ii)'s is not** (2 ulp): at x = 0.05, Q² = 2.5 it runs −5.4705 % (y = 0.1) → +2.3678 % (y = 0.9), straddling zero, so a single-y quotation of (iii) is not a statement about a run. And unlike `--b1-unpol` it moves the **unpolarised rate**: σ_pb −0.684681 % on the 2000-event run. Refused off `--b1-model li6-convolution`. Gated by `tests/test_b1_nuclear.cpp` T18, `test_b1_model.py` P10 (6 tests) and three `test_knob_provenance.py` matrix cells. **No default moved and no reference JSON moved** (`git diff --stat validation/reference/` empty). End state re-measured: **402 doctest cases / 17 240 344 assertions / 1 skipped / 0 failed**, **941 pytest passed / 124 skipped**, gate **1216 strict / 96 ranges / 7 external / 0 broken / 6 allow-listed** (+19/115 SPIN32, +8 external PYTHIA_BRIDGE), SPDX **101/101** — every delta an addition, itemised in §A1.9. | `phase_A_li6_tables.md`; `phase_A_numbers.md` §A3, §A1 |
| CW **bug fix, unplanned** — the tagged sector's S–D interference sign | DONE | a7b3d18 | **The one default that moved this run, and it moved because it was wrong.** `docs/benchmarking/07_cw_sign_investigation.md` returned the tagged S–D sign **refuted at blocker severity**; the fix is **one line** in `src/core/tagged.cpp` `build_amp2` — the phase `(-1)^floor(L/2)`, the observable relative part of **φ_L = i^L ψ_L**, which was missing entirely — plus the two doc comments that carried the false inference. `ψ₂ = +W` is the STORED convention and is right; `build_amp2` consumes φ, not ψ, and `rad_[2]` / `Wave::psi()` are NOT negated, so the two tests that assert that convention still pass. **Measured, before → after:** ⁶Li `A_zz^tag(k = 0.20 GeV)` acceptance-weighted at the YR high-acceptance optics **+0.8450 → −1.2069** (Hulthén β = 0.30) and **+0.4518 → −0.5191** (VMC AV18) — and the corrected VMC column is `vmc_reconciliation.md`'s **`vmc-flipD` control column, digit for digit**: the tree's designated control on the S–D sign was the physics. **Cosyn–Weiss II Eq. (6.12) now holds as an IDENTITY** over all 280 × 96 cells on both the AV18 and Hulthén deuteron controls, `max\|A_zz − CW\| = 8.881784e−16` against **2.740499e+00** before (re-measured independently 2026-09-06 over all 280 × 96 = 26 880 cells), and TABLE II's three rows come out **−1.937124 / +0.999313 / +0.967340** against CW's −2 / +1 / +1 (the residuals are the finite cell centres; through the exact P₂ factorization the envelope extremes are −1.999864 and +0.999997 at CW's own k = 0.2968 and 1.0366 GeV). **The mapping is `A_T∥ = +1 · A_zz^wf`, not −2** — CW's −2 is a node at θ_k = 0, not a conversion factor, and the old gate double-counted it. **⁷Li α-tag does not move at all** (one L = 1 wave, the common i is global): `n_of_kc` and `p2_moment` **bit-identical at 17 digits**. **Neither does the ⁶Li α-tag RATE** — the spin-blind accepted fraction is unmoved to **≤ 4.0e−16 relative** (1–2 ulp; VMC bit-identical): **0.0246759321488** Hulthén and **0.0338102276258** VMC; what flips is the (M, m_S, cos θ_k) correlation `sample_kc` draws, which is the observable. `p2_moment` flips sign on every spin-1 channel (×1.28 to ×2.03); the dilutions move only by their 96-cell quadrature residual (≤ 4.3e−6), so `LI6_B1_RANK2_TRANSFER`, the effective polarizations and **the whole b₁ and inclusive sector are untouched** — `b1_default_li6.json` byte-identical. **Amended 2026-09-06 (verification pass): "the tag fractions" does not belong in that list.** A tag fraction is an acceptance-weighted integral over n_M, so it is invariant only for a spin-blind or category-averaged fill: the uniform-M mix fraction is unmoved to all 15 digits (0.024675932148828 / 0.033810227625842) and the `tensor-thirds` equal-thirds average reproduces it to +3.1e−9, but the categories themselves go **0.028127 → 0.018403** (azz±) and **0.017775 → 0.037222** (azz0) and the CLI's own default `--pz 0.7` ladder goes **0.027030 → 0.020396**, −24.5 % (⁶Li Hulthén, YR high-acceptance, `n_phi = 32`). The 40 k-event sampled rows in `USAGE.md` and `vmc_reconciliation.md` are re-drawn by ~1.3σ and were REGENERATED on the fixed build; the `--cluster-wave` CLI help, `07` §6.1/§6.5 and `phase_CW_numbers.md` §7.1/§7.2 now carry the measured statement. Also corrected in that pass: the RC sector is spin-dependent and DOES move (`RcModel::tagged_tau` reads `n_of_kc` per M), the re-pin record's `norm` / `population_integrated` magnitudes (5.8e−5 rel and 3.0e−6 abs, not 2e−5 and 5e−7) plus the `p2_moment_mixture_uniform` it omitted, and the one widened test tolerance (CW gate TABLE II θ ≈ 90°, 1e−3 → 2e−3, measured residual 2.77e−6) — recorded in `phase_CW_numbers.md` §9. **`validation/reference/tagged.json` was DELIBERATELY re-pinned**: polligen's own `tagged._amp2_table` carries the same missing `i^L`, so the reference cannot be regenerated from it; the two spin-1 `model` blocks are dumped from the fixed C++ by the new `validation/repin_tagged_from_lipolgen.py`, provenance **"LiPolGen post-fix, formerly polligen"**, recorded in `validation/README.md`, with `dump_polligen_reference.py` taught to carry them through instead of overwriting them. **⁷Li's block stays polligen's, byte for byte** (re-checked at rtol 1e-12), and so does everything outside `model`. **No other reference JSON moved** (SHA-256 verified, seven files). Four tests re-pinned as the investigation named, plus a fifth it did not (T25's dilutions, 1.4e−6 past a 1e−6 gate); the Cosyn–Weiss gate is **rewritten on the AV18 control CW actually used** and carries a regression guard. **Sites rewritten, first pass:** `USAGE.md`, `CONVENTIONS.md`, `PHYSICS_CHANNELS.md`, `vmc_reconciliation.md` (dated record kept, correction box added), `validation/README.md`. **Second pass — every remaining site that quotes an inheriting number, live or dated** (`phase_CW_numbers.md` §10, second table): live sites get the corrected number with the fix named and dated — `OPEN_ITEMS_SOLUTIONS.md` §1 (two bullets; *"roughly halves"* **withdrawn as a reading** — the measured VMC/Hulthén ratio is a factor 2.3 at k = 0.20 falling to 8.8 at k = 0.40, not a halving) and §11.4, `README.md`'s external-anchors line (the `+1/−2` mapping corrected), `DEVELOPMENT_PLAN.md` §4 row 4, the `--cluster-wave` **CLI help** (a user-facing SIGN NOTE: any A_zz^tag from a pre-2026-09-06 run has the wrong sign) and `configs.py`'s docstring (which records that it does **not** inherit, and why). Dated records keep their originals and gain a correction box: `BENCHMARK_PLAN.md` row 2 / §5 item 1 (**RESOLVED**), `00_in_tree_checks.md` **E-1**, `06_critic.md` **U-2**, `04_theory.md` **T-6**, `needs_survey.md` §5.2/§5/anchor table (it told a replacement generator to reproduce the wrong gate; now says **do not**), `run_2026-09-03/phase_C_numbers.md` §C5.2 and §C5.4 — **§C5.4 is a THIRD site carrying the false step**, which `07` §5 did not enumerate — `AUTHOR_DECISIONS.md` rows 9/10/11 (**row 9's evidence moved**: its "the control's job is to be the Cosyn–Weiss tagged limit" leg now argues the other way, since the gate runs on AV18; no decision taken) and `run_2026-09-03/SUMMARY.md` (new finding **6b**). One published digit corrected on re-measurement: the VMC accepted fraction, first published here as `0.033810227625845`, is `0.033810227625842` — quote no more than `0.0338102276258`. `PolarizedLithiumSim` **NOT modified** (verified: its working tree is clean at `2a27972`); what inherits the bug there is itemised in **§11** — **eight** sites with file:line and the measured number, chiefly `evgen/polligen/tagged.py:247` (the same missing `i^L`), `evgen/scripts/money_tagged_azz.py` and its published figure `evgen/money_tagged_azz_6Li.png`, and `evgen/README.md:113-115`'s *"the 90° curve says −0.48"*, which re-measures as **+0.9279** — plus **seven** checked-safe ones, notably `spectator.py` (spin-blind by construction) and `LI6_B1_RANK2_TRANSFER` (+2.46e−6 against a 1e−4 pin). Raised as a **registry-row candidate** below. End state: **404 doctest / 17 240 391 assertions / 1 skipped / 0 failed**, **1004 pytest passed / 127 skipped**, gate **1220 strict / 96 ranges / 7 external / 0 broken / 6 allow-listed**, SPDX **102/102**. | `phase_CW_numbers.md`; `../../benchmarking/07_cw_sign_investigation.md` |
| B s/p tensor bound; `PolradFull`; Q(⁷Li) sourced | DONE | af3f415 | **B3 (2026-09-06): Q(⁷Li) is sourced, committed and gated.** `SUMMARY.md` item 4's *"quoted from memory and absent from this tree"* is closed. **Fetched** (network used for this item only, three published documents, exact URLs and what was read in `phase_B_numbers.md` §B3.1): TUNL's `ourpubs/07_2002.pdf` and `06_2002.pdf` — the A = 7 and A = 6 halves of **one** paper, Tilley *et al.*, *Energy Levels of Light Nuclei, A = 5, 6, 7*, **NPA 708 (2002) 3** — and the NNDC mirror of **N. J. Stone**'s *Table of Nuclear Magnetic Dipole and Electric Quadrupole Moments* (04/11/2001). The A = 7 half prints `Q = −40.6 ± 0.8 mb (1988DI1B)`; the A = 6 half prints the `Q = −0.818(17) mb (1998CE04)` that **is** `LI6_QUADRUPOLE_FM2`. **Adopted `LI7_QUADRUPOLE_FM2` = −4.06 fm²** at `rc.hpp:669`, beside `LI6_QUADRUPOLE_FM2` — **one home, one document, one sign convention (spectroscopic, m = I), one unit rule (1 mb = 0.1 fm²)**, which is what the brief's *match conventions* required; the −4.00(3) fm² the research note quoted is one of the **eight** ⁷Li determinations Stone lists without recommending any, and all eight are transcribed into `rc.hpp` with the swap price. (The IAEA `indc-nds-0833.pdf` returned **HTTP 402** and is quoted nowhere.) **§4.2's gate is now real code**: `li7_alpha_t_quadrupole` / `alpha_t_quadrupole` / `struct AlphaTQuadrupole` in `b1_nuclear.hpp`, the L = 1 Fourier–Bessel over `momenta/li7_at3.momentum` with Z_eff from `nuclear_mass` and the parameter-free −(2/5); it **reproduces the research note's own offline numpy recipe to the LAST BIT** (relative difference **0.000e+00** on ⟨r²⟩, Q and Z_eff). Measured: ⟨r²⟩ = **12.534828961030 fm²**, r_rms = **3.540456038568 fm**, Z_eff = **0.695075101362**, **Q = −3.485059004257 fm²**; grid −3.472004 / −3.485059 / −3.485797 / −3.486171 at r_max = 20/30/40/60 fm (**0.032 %** from 30 to 60), VMC MC band **±0.33 %** (−3.496427 / −3.473745 at σ = ∓1), quadrature stable to **4e−9**. **The gate is a REPORTED RATIO: 0.858389 against −4.06 (14.16 % low) and 0.871265 against −4.00 (12.87 % low)** — the note's 13 % is the second; the compilation choice is worth 1.5 % and does not decide the verdict *13–14 % low*. The ⁶Li contrast from the same code family is a factor **4.0748** (307.5 %), so ⁷Li is better by **21.7×** — *an order of magnitude better* is a measurement, asserted with a threshold of 10. **It validates the α–t WAVE FUNCTION's quadrupole only.** b₁(⁷Li) is **not implemented** (open item 15, blocked on D2 / row 3), ⁷Li's rank-2 sector is still exactly zero, and pytest `test_the_passing_wave_function_gate_ships_no_b1_at_all` re-asserts that zero **in the very test that runs the gate**. Said at every site: `rc.hpp`'s constant comment, `b1_nuclear.hpp`'s declaration comment, T13's header and `MESSAGE`, both binding docstrings, and G8's docstrings. **No option, no CLI flag, no `knob_provenance` row** — nothing here is a knob; **no default moved, no shipped number moved, no reference JSON moved.** End state re-measured: **405 doctest cases / 17 240 417 assertions / 1 skipped / 0 failed** (+1 case, +26 assertions = T13), **1008 pytest passed / 127 skipped** (+4 = G8), gate **1224 strict / 96 ranges / 7 external / 0 broken / 6 allow-listed** (+19/115 SPIN32, +8 external PYTHIA_BRIDGE) — **1220 + the 4 citations this task added** (`LI7_QUADRUPOLE_FM2`, `AlphaTQuadrupole`, `alpha_t_quadrupole`, `li7_alpha_t_quadrupole`, all on `PHYSICS_CHANNELS.md`'s spin-3/2 row), ranges/external/broken/allowed unchanged. The line shifts were absorbed by `check_physics_channels_links.py --fix`: **60** named citations re-pointed for `rc.hpp`'s 53 new lines and `bindings.cpp`'s 24 (58 in `PHYSICS_CHANNELS.md` — 55 into `rc.hpp`, 3 into `bindings.cpp` — and 2 range citations in `SPIN32_FINITE_GAMMA.md`), plus **6** `USAGE.md` range citations for that file's own 6 new lines — 4 by `--fix` and **2 by hand, re-pointed by CONTENT** (§7 *Checks you get for free*, §8 *Command-line generators*: `--fix` reported them as starting/ending on a blank line rather than moving them, so their citation and their sidecar key were moved to the block whose recorded `first`/`last` they still match, and the stored sha256 was left untouched and re-verified). **No citation deleted, no cited block edited, no allow-list entry added, `--record-ranges` never run.** SPDX **102/102** (no new source file). **B1 (2026-09-06): the tensor fraction of the s-/p-peaks is BOUNDED, NOT COMPUTED — and the number that goes with the word is that the bound is EMPTY where the run is quoted.** `SUMMARY.md` item 5's *"unknown, not zero"* is closed at all **five** sites the brief names (`SUMMARY.md`, `OPEN_ITEMS_SOLUTIONS.md` §9, `rc.hpp`, `USAGE.md` §7b, `PHYSICS_CHANNELS.md` — `rc.hpp` and `USAGE.md` carried it twice each) and at **four more the brief does not** (`docs/CONVENTIONS.md`'s refuse/label list, `python/lipolgen/__init__.py`'s `RC_TAIL_MODELS` note, `cli.py`'s run banner, and T8(d)'s own comment in `tests/test_rc.cpp`) — closed **with its number**, not with the word alone. **Shipped:** `RcOptions::sp_tensor_scale` / `--rc-sp-tensor-scale` (default **0.0**) adds `(Q_N/6)·sp_tensor_scale·ratio_t·u_sp` to `tail_ratio_at`'s numerator, **outside** `qe_suppression` because `u_sp` is the ELASTIC s+p column, so scale 1 is exactly *"the s/p tensor fraction equals the elastic t-peak's"*. **REFUSED unless `tail_model = TPeakPlusLL`** (validate() and `RcModel`'s ctor, the `with_qe_tail` precedent): under the shipped `t-peak` the `u_sp` table is identically zero. Its `knob_provenance` row is three-way — `not-read` at `--rc off`, **`refused`** at `t-peak`, `read` at `t-peak+ll`, `refused` on the tagged/coherent channels — and both live cells are in the pytest matrix, the first **moving** the 60-event output hash. **Priced (⁶Li config 1, production grid, Q² = 5):** `t-peak → t-peak+ll` collapses the tensor fraction `r_T/r_U` by **×0.66139 / ×0.0031829 / ×6.6076e−05** at x = 0.01 / 0.10 / 0.30, because `r_U` grows **×1.512 / ×314.18 / ×15134** while the tensor numerator does not move. Against that, **Δ(ΔA_zz) at sp = 0.5 and 1 is EXACTLY ZERO at all three x** — bit-identical for `q_n` = 0, +1, −2 — because the coherent s/p vertex sits at Q′²_s = **4.3728 / 4.9382 / 4.9801 GeV²**, where `F_c` = −3.75e−45 / −6.44e−51 / −2.41e−51 against **+2.99260** at the t-peak's own `t_min` = 8.7304e−05, so `u_sp/σ^el_U` = 5.25e−79 / 2.07e−86 / 4.06e−83 and the added term is below the numerator's ulp. For contrast on the same edge, `--rc-qe-tensor-scale 1` moves ΔA_zz by **0.0855 % / 0.00031 % / 0.246 %** of the band half-width. The knob **is** alive on **77 of 3051** accepted cells (all x ≤ 7.943e−03, y ≥ 0.3656), reaching **582.9 %** of the band half-width at x = 4.169e−04, Q² = 1.608, y = 0.9692 (qe there: 328.4 %) — and that is the corner `TPeakPlusLL`'s own uncancelled soft radiator makes untrustworthy, so it is a price tag, not a systematic to quote. **It recovers at most 21.72 % of the collapse anywhere on the grid, and 0.00 % at the three standard points.** Exactly LINEAR (0.5 / 2 / 4 give 0.5× / 2× / 4×, `4.00000000000639` at the worst cell; linear in `Q_N` at 1e−9), so one run rescales. 200 k mean `rc_tail` (fill P_z = 0.7, the CLI default — named 2026-09-15) 1.040274188 → 1.040274193 (sp = 1) → 1.040274711 (sp = 100), max 5.981595247 → 5.981588558, 0 clipped. **What B1 does NOT bound, named and gated:** the **QUASI-ELASTIC s/p column** `TailTriple::qe_sp` — `qe_tensor_scale` multiplies σ^q_U alone and `sp_tensor_scale` the elastic s/p column alone — keeps a tensor part of exactly zero and is essentially the **whole** of the ×314 / ×15134 collapse. **It is now the largest exactly-zero tensor term in `rc_tail` and no knob reaches it**; `test_the_quasi_elastic_sp_column_is_bounded_by_NEITHER_scale` asserts the gap so it cannot be quietly closed or forgotten. **NO DEFAULT MOVED, measured not argued:** all six edited sources were reverted in place, the library rebuilt and three npz regenerated at seed 4242 — **every numeric column byte-identical** on `--rc off` (48/48), `--rc tensor-band` (51/51) and `t-peak+ll` (51/51); `meta` gains only `rc_sp_tensor_scale` and one `knob_provenance` row (68 → 69, **no existing row changed**); the 200 k seed-1234 `--rc tensor-band` run is **byte-identical between the two builds**; `git diff --stat validation/reference/` is empty. End state: **406 doctest cases / 17 241 655 assertions / 1 skipped / 0 failed** (+1 case, +1238 assertions), **1016 passed / 136 skipped**, gate **1228 strict / 96 ranges / 7 external / 0 broken / 6 allow-listed** (+4 = the new `PHYSICS_CHANNELS.md` row's citations), SPDX **102/102** (no new source file). **A bookkeeping defect was found on the way and fixed:** `PHYSICS_CHANNELS.md` cited `docs/USAGE.md:2152-2327` as "§9", but that range has pointed into **§7b** for some time (§9 starts at line 2652 and runs to EOF at 2849); the fingerprint gate could not see it because the recorded first/last lines still matched, and it surfaced only because a B1 edit landed inside the range. Re-pointed to **`docs/USAGE.md:2652-2849`**. The other 18 shifted use-site citations were re-pointed by a `difflib` old→new line map against pre-edit copies, and `--record-ranges` was run twice, deliberately (that range, and the new `rc.cpp:1010` use-site fingerprint); two further `USAGE.md` range citations (§8, §9) were re-pointed **by content** with their stored sha256 untouched, and `docs/CONVENTIONS.md`'s edit was kept **line-neutral** so that the nine range citations pointing into it never moved. **B2 (2026-09-06): `RcTailModel::PolradFull` — POLRAD Eq. (18) + Appendix B + Eq. (A.4) — IS IMPLEMENTED, opt-in as `--rc-tail-model polrad-full`** (`phase_B_numbers.md` §B2). It threw from 2026-09-02 and `tests/test_rc.cpp` **T9** was the suite's one unconditional `doctest::skip`; both are gone (408 cases, **0 skipped**). **THE HEADLINE: the exact tail is NOT inside the band the two t-peak models were being run as** — over the sampler's 3051 accepted cells it lies between them on **1725 (56.5 %)** and outside on **1326 (43.5 %)**, covers a median **0.6555** of the gap **over the 3027 cells whose gap is nonzero** (0.6620 over all 3051; the other 24 have `t-peak+ll` = `t-peak` exactly), the **ratio of the σ-weighted mean shifts** is **0.428** — a ratio of means, *not* a σ-weighted mean of the per-cell fractions, which is 6.483 — and in the Q² ≥ 20, y ≤ 0.9 window it is **above both** (8.815031e−03 against 8.719649e−03 / 8.773752e−03, at the default fill P_z = 0.7; 8.581236e−03 against 8.489138e−03 / 8.540716e−03 at P_z = 0). Whole-run mean `rc_tail`, 200 k, seed 1234, **at the default fill P_z = 0.7**: **1.021778527 → 1.040274188 → 1.029702912**, and at **P_z = 0** (the plan both suites use) **1.021836305 → 1.040368933 → 1.029775347** (*amended 2026-09-15 — this row named no P_z, and an event-weighted mean is a statement about one; the cell-σ-weighted census is P_z-free*). **SECOND HEADLINE, a physics correction to a shipped sentence:** *"POLRAD supplies no tensor s/p peak"* is true of **Eq. (38)** and **false of the paper** — Eq. (18) + Eq. (A.4) carries it, `PolradFull` computes it, and `--rc-sp-tensor-scale` is refused there for the **opposite** reason it is refused on `t-peak` (the term RAN). Scored against that computed answer, B1's bound was **not even one-sided**: `r_T/r_U` × the t-peak is **×0.79395 / ×0.0020032 / ×0.00022591** (full) against ×0.66139 / ×0.0031829 / ×6.6076e−05 (t+ll) at x = 0.01 / 0.10 / 0.30 — right way and far short at 0.01 and 0.30, **wrong way** at 0.10. **THE GATES.** (i) the limit is stated and measured — `x_A → 0` with a form factor dead at the s-/p-peak vertex gives Eq. (18)/Eq. (38) = **1.00230** (unpol.) / **1.00313** (tensor) at x_A = 0.003, tending to 1, on three form-factor sectors run separately; that also *measures* the `1/A²` reduction the transcription check could only infer. (ii) **T9 un-skipped**, gating Eq. (A.4)'s Q_N = 0 Rosenbluth limit through the shipped `polrad_im_el_spin1` (and exercising §7.1's `ℑ₆` correction for the first time). (iii) the transcription check's own three deuteron points. **FIVE TRANSCRIPTION DEFECTS in `polrad2t.tex`'s Appendix B** — never transcribed before because nothing evaluated it — found by that gate and settled against the FORTRAN, recorded in `run_2026-09-02/polrad_transcription_check.md` **§10** (new). **The HERMES deuteron point is settled**: the t-peak is low by **2.36×**, not the 4.36× the leading-log edge claimed — the upper edge **overshoots by 1.85×**. **WHAT IT DOES NOT CLOSE, said at every site:** the **quasi-elastic** tensor tail is zero on all three models (Eq. (A.5) → `Im₅…₈ ≡ 0` for spin-½), and that is the column carrying ×202 and ×7123 of the t-peak at x = 0.10 / 0.30; `qe_tensor_scale` is still the only stand-in. **And it is still NOT validated against Mo–Tsai** — that paper is not in this tree. `m_lepton`'s reservation is discharged (read on `polrad-full`, refused on the two t-peak models, `not-read` at `--rc off` — the old unconditional `refused` was false there, since validate()'s rc block sits inside `if (rc != Off)`). **NO DEFAULT MOVED:** 51/51 numeric columns byte-identical on `t-peak` and `t-peak+ll` at 200 k seed 1234 against the pre-B2 build's own npz, 48/48 at `--rc off`, `knob_provenance` 69 → 69 rows with **two reason texts** changed and no status or value; `git diff --stat validation/reference/` empty. Suites 406 → **408** cases / 17 241 655 → **17 241 949** assertions / **1 → 0 skipped** (**17 241 975** after the 2026-09-06 verification pass added T19(i), §B2.12); pytest 1016 → **1023** passed, 136 → **151** skipped; docs gate 1228 → **1236** refs, **0 broken**; SPDX 102/102, no new source file. **REGISTRY: no new row** — `polrad-full` is a new *value* of the `tail_model` knob `run_2026-09-03/AUTHOR_DECISIONS.md` §B15 already lists as *not* a decision, and every default is byte-identical. What it leaves for a future row is sharper than B1 could put it: there is now a **computed** third answer, it is outside the shipped pair on 43.5 % of cells, and whether the default should stay `t-peak` is an author call **nobody here made** (price: event-weighted `rc_tail` 1.021778527 → 1.029702912 at P_z = 0.7, 1.021836305 → 1.029775347 at P_z = 0 — **+0.776 %** / **+0.777 %**, re-measured 2026-09-15; the "+0.79 %" this row printed is the *cell-σ-weighted* pair's +0.7871 %). **VERIFICATION PASS (2026-09-06), four residues closed — §B2.12.** (i) `PolradFull` had **no pinned VALUE** anywhere: replacing the per-nucleon `1/A²` by `1/A` in `RcModel::tail_sigma_at`'s `PolradFull` branch — the exact historical factor-A bug at the exact line — left T9 + T19 at **316/316** and both `polrad-full` pytests green. **T19(i)** and a mirror block in `test_rc.py` now pin `tail_sigma_at` to values (σ^el_U = **2.2301252856028388e−05** at x = 0.01, Q² = 5, which is the raw 8.0284510281702199e−04 **/36**, plus an x ≠ x_A point and a quasi-elastic-dominated one, rtol 1e−9); the perturbation now fails **12** assertions and the pytest, verified with `rc.cpp` restored to sha256 `24a2238e…`. (ii) B2's own physics correction (*"POLRAD supplies no tensor s/p peak"* is true of Eq. (38) and **false of the paper**) was **not propagated**: qualified now at all **eleven** shipped sites, with correction boxes on three dated records and dated notes on `design_C_tensor_rc.md`'s `NOT IMPLEMENTED` sketch. (iii) The census published in `rc.hpp`, `cli.py` and T19(g) was **the ceiling mislabelled**: 1.02217080 → 1.04097066 → **1.03020526** is **cell-σ-weighted and clipped**, not event-weighted (event-weighted at the default fill P_z = 0.7 is 1.021778527 → 1.040274188 → **1.029702912**, and at P_z = 0 it is 1.021836305 → 1.040368933 → **1.029775347**; unclipped cell-σ-weighted is **1.03021634** on either), and "**×11** at x = 0.955, Q² = 186" is **1 + `tail_max`** with **six** cells tied at it — the model's own worst is **×4518.3** at x = 0.954993, **Q² = 206.68**, and the Q² = 186 cell is ×3449.9. All re-measured here and corrected at every site. (iv) Seven `adgh` line citations, `rc.hpp`'s 1.00493/1.01998 (measured **1.00492**/**1.01997**) and its "wrong in **three** places" (it is **five**) corrected. **No code changed** — every edit outside `tests/` is a comment, docstring or `help=` string; `--rc off` and the `TPeak` default are unmoved. **AMENDED 2026-09-15 (residue pass), §B2.5's correction boxes — one claim of the verification pass was FALSE and is withdrawn.** The pass reported that the window absolutes *"5182 … 8.48914e−03 → 8.54072e−03"* "no longer reproduce" and blamed Phase A for moving the ⁶Li kernel normalisation and with it the sampler's cell weights. **They reproduce exactly**, at `--pz 0`: re-measured on this build, `tensor_thirds_plan(0.0, 0.6)` gives **5182** / **8.489138e−03 → 8.540716e−03 → 8.581236e−03** and run means **1.021836305 / 1.040368933 / 1.029775347**, while `tensor_thirds_plan(0.7, 0.6)` — the CLI default the re-measurement had silently switched to — gives **5194** / **8.719649e−03 → 8.773752e−03 → 8.815031e−03** and **1.021778527 / 1.040274188 / 1.029702912**. `run_2026-09-03/phase_B_numbers.md:836-839` had already tabulated both. **Phase A moved nothing here**; the false attribution is corrected and P_z named at every site that carries the triple. Also corrected on the same pass: **two more `adgh` off-by-ones** (`adgh:1114-1115` → **`1115-1116`** for the bare τ/M² term, `adgh:1102-1107` → **`1101-1105`** for the `ajm2`/`ajm3` block), the **"0.428 σ-weighted"** label (it is the RATIO of σ-weighted mean *shifts*, not a mean of per-cell fractions — that is 6.483 — and three sites called it "event-weighted"), and the **median 0.6555**, which is over the **3027** cells with a nonzero gap (0.6620 over all 3051). **Still no code changed.** **Final (2026-09-15): the P_z attribution withdrawn at the last live site (PHYSICS_CHANNELS.md elastic-tail row), P_z named on every event-weighted figure, the F_m-only sector ratios labelled as such. [doctest] test cases:      408 |      408 passed | 0 failed | 0 skipped [doctest] assertions: 17241975 | 17241975 passed | 0 failed |; 1023 passed, 151 skipped; gate 0 broken on all three documents; SPDX 102; validation/reference/ untouched.** | `phase_B_numbers.md` §B1, §B2, §B2.5, §B2.12, §B3 |
| C lepton-efficiency bound from the sibling fastsim | DONE (negative) | – | **No defensible bound exists** on the per-lepton central-detector reconstruction efficiency anywhere in the sibling (the '0.95/track' is `polligen.hfs.HadronResponse`'s labelled stand-in, `evgen/polligen/hfs.py:251`; the only lepton-ID number in 54 refs is ATHENA's barrel electron efficiency, not ePIC, not reconstruction, not muons); the factor stays UNBOUNDED, stated at all eight sites. Two incidental findings applied: the `HfsModel` citation named a class that does not exist (→ `HadronResponse`, three sites); and **`COHERENT_JPSI_EFF_IR8_LI7` = 0.1775 is a pure geometric acceptance, hence an UPPER bound on the recoil-leg efficiency × acceptance** (Chang et al. §IV: no detector efficiency, no reconstruction efficiency/acceptance) — direction DOWN, unquantified, compounding with the beam-energy leg; recorded at the constant's home, the O5 script and §11.3. Whether to fold a lower value in is registry §B25's. Survey: `phase_C_survey.md`. |η| < 1, so it covers 45 % of them and has no muon counterpart. **It does not bound ε_pair.** **So: no constant added to `coherent.hpp`, no fourth ladder row, self-check not re-keyed, band untouched at both ends** (2.618/13.1 headline; LOW 2.627 at 13.0; TOP 2.842–3.292 at 11.1–8.3; optics 0.734/0.794/0.920 at 167.1/142.7/106.4 — all bit-identical; `self-check: OK`). **The count stays FOUR**, with the second of the four now *surveyed* rather than merely asserted: one sentence naming what was looked at was added at all eight enumerated sites (`o5_a2_reach.py`, `run_2026-09-03/phase_C_numbers.md` §C2, `OPEN_ITEMS_SOLUTIONS.md` §11.3, `cluster_config.hpp` `a2_from_geometry`, `configs.py`, the Mäntysaari draft, `SUMMARY.md`, `AUTHOR_DECISIONS.md` §B25). Adding those lines drifted 12 point citations in `PHYSICS_CHANNELS.md`; the gate caught all 12 (rc 1) and `--fix` relocated each onto the declaration it already named — **line numbers only, two table rows, no range re-recorded**. **TWO AUTHOR DECISIONS RAISED, NEITHER APPLIED** (`phase_C_numbers.md` §C5): (1) `HfsModel` **names no class in the sibling** — 0 grep hits against 26 for `HadronResponse` — so the citation at `o5_a2_reach.py:437`/`:466` and in two docs is unresolvable as written, though its substance is right; (2) **a BOUND was found on the neighbouring quantity** — by arXiv:2511.05638's own §IV (*"only accounts for the acceptance effect and does not incorporate the efficiencies of the detector … nor the efficiency and acceptance of the reconstructed distribution"*), `COHERENT_JPSI_EFF_IR8_LI7` = 0.1775 is an **UPPER bound** on the far-forward intact-⁷Li tagging efficiency × acceptance, i.e. a pure geometric acceptance with no Roman-Pot detector efficiency on the **recoil** leg either; direction **DOWN**, and it is a *third* factor where the chain's stated ×1.1224-UP / ×0.897-DOWN balance has room for two — it **compounds with the decay-lepton omission and breaks that cancellation** rather than being cancelled by the beam-energy leg. Suites/gates unchanged: 408/408, 17 241 975 assertions; 1023 passed + 151 skipped; SPDX 102/102; links 1236/96/7/0 + 19/115/0/0 + 0/0/8/0, rc 0. Record: `phase_C_numbers.md`; survey: `phase_C_survey.md`. |
| D gate residue class | TODO | – | |
| E close-out | TODO | – | |

## New registry rows surfaced by this run

A row is added only for a genuinely new decision; costs priced here go into
the EXISTING rows of `../run_2026-09-03/AUTHOR_DECISIONS.md`.

**B1 (2026-09-06) added NO row.** `sp_tensor_scale` defaults to 0, every
default is bit-identical, and it is the direct sibling of `qe_tensor_scale`,
which `../run_2026-09-03/AUTHOR_DECISIONS.md` §B15 already lists as **not** an
author decision — a knob that moves nothing at the CLI default whichever way
the author would go. Its price is recorded **into that existing §B15**, in
(b), (d) and (e). If the author wants §B15's family decided, §B15 becomes
registry row 26; **nobody here made that call.** B1 does surface one thing a
future row may want: the **quasi-elastic s/p tensor column** is now the largest
exactly-zero tensor term in `rc_tail` and no knob reaches it
(`phase_B_numbers.md` §B1.5).

**B3 (2026-09-06) added NO row.** It pays the existing **D11** of
`../run_2026-09-03/AUTHOR_DECISIONS.md` §B20 (`run_2026-09-03/STATUS.md`
decision row 20 / `OPEN_ITEMS_SOLUTIONS.md` §15.5), whose "what changes when
decided" column had already named `LI7_QUADRUPOLE_FM2` beside
`LI6_QUADRUPOLE_FM2` in `rc.hpp` and §4.2's gate, and whose predicted price
`ratio 0.871 → 0.858` is confirmed to six digits (0.871265 → 0.858389). §B20
carries a **CLOSED FOR ROW 20's D11** paragraph; D1, D3–D10 and D12 of that
row are untouched and the row itself stays open on them.

### CANDIDATE — the sibling's `A_zz^tag` numbers must be regenerated

**Surfaced 2026-09-06 by the CW bug fix.** This is a decision because it is
about **another repository**, which this run is not permitted to modify and
which carries a **published figure**; it is not a cost that fits an existing
row.

**(a) The question.** `PolarizedLithiumSim`'s `evgen/polligen/tagged.py:247`
carries the **same missing `i^L`** that this tree fixed — its
`TaggedModel._amp2_table` sums ψ_L into a partial-wave amplitude that needs
φ_L = i^L ψ_L. Every angle-differential spin-1 tagged quantity there is
therefore inverted, including **`evgen/money_tagged_azz_6Li.png`, a published
figure**, and `evgen/README.md:113-115`, which quotes *"the folded A_zz reads
+0.49 and −0.07 at k ≈ 0.33 GeV/c where the 90° curve says −0.48"* — the −0.48
reproduces here as **−0.4786** pre-fix and is **+0.9279** after. Who fixes it,
and does the figure get regenerated or withdrawn?

**(b) Options.** (i) fix `tagged.py:247` in the sibling and regenerate money
plot 4, its README sentence and the five affected pins in
`evgen/tests/test_tagged.py`; (ii) fix the code but leave the figure, flagged;
(iii) leave the sibling alone and record the divergence only (**status quo as
of this run** — `PolarizedLithiumSim` was **not** modified).

**(c) Evidence.** `phase_CW_numbers.md` §11 itemises it: **eight** inheriting
sites with file:line and the measured number, and **seven** checked-safe ones
— notably `fastsim/polli_fastsim/spectator.py`, which is spin-blind by
construction (no M, no Clebsch–Gordan, no interference term in the file), and
`fastsim/polli_fastsim/polarized.py:873` `LI6_B1_RANK2_TRANSFER = 0.921947`,
which moves **+2.46e−6 relative** against a **1e−4** pin and so does **not**
inherit a meaningful error. ⁷Li is untouched everywhere (one L = 1 wave).
The sibling's `evgen/tests/test_tagged.py:737-790`
`test_cosyn_weiss_tensor_gate` is **wrong twice over** — the `−2` mapping and
the Hulthén channel — and is why the bug survived review on both sides.

**(d) Cost of doing nothing.** The two trees now disagree on every
angle-differential spin-1 tagged quantity, and **this tree is the one that
agrees with Cosyn–Weiss Eq. (6.12)** — as an identity, `8.881784e−16` over
26 880 cells against `2.740499e+00`. `validation/reference/tagged.json` has
already stopped tracking polligen for the two spin-1 `model` blocks (§8), so
the port gate no longer catches a future divergence there.

**(e) Not taken here.** No file in `/home/cpeng/Projects/polli/PolarizedLithiumSim`
was read-modified; this row records the finding for the author.
