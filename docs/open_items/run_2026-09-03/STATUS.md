# Run 2026-09-03 — status

Started 2026-09-03 by the supervising session, after a 14-agent inventory of the
whole tree (44 open items across six domains). Plan: `PLAN.md` next to this file.
Baseline at start: `a94fd6e`, clean, **363 doctest / 17 203 863 assertions /
1 skipped**, **211 pytest** (35.75 s).

| phase | state | commit | notes |
|---|---|---|---|
| A b₁ publication ban (item 10, conditions 1–7) | DONE | – | A1 `MstwSF` over the on-disk PYTHIA grid; A2 gate rerun — **G3b passes at 0.843243 for the MSTW2008 LO nucleon input at CDKS Eq. (21)'s δ-function, and NOT for the shipped `ToyF2` default, which stays at 0.440, outside [0.5, 2]**; A3 the normalisation arbiter (HERMES b₁ᵈ is per nucleon); A5 CD-Bonn — 1.000338, below the reference figure's own digitization error and specific to CD-Bonn *and* MSTW together; A4/A6/A7 the CDKS normalisation applied, the r-default kept, the ban lifted **for the measured configuration only** across 18 files. Fix round: `--b1-unpol {toy,ct18nlo,mstw}` makes that configuration reachable (it was not — the pipeline hard-wired `ToyF2`), G3a's counting ceiling recorded as a qualified pass. **374 doctest cases / 17 208 032 assertions / 1 skipped, 261 pytest, 1049 refs 0 broken** |
| B radiative tail correctness (item 9) | IN PROGRESS (B1–B6 on disk, uncommitted) | – | **B1** `C0Shape{Ho,VmcFt}` — the ⁶Li C0 shape ships as a band and the published sign of `(1/6)σ^el_T/σ^el_U` at x = 0.10 is withdrawn as a band edge. **B2** the leading-log s-/p-peaks promoted into `src/core/rc.cpp` and `RcTailModel::TPeakPlusLL`; the “< 1 % at Q² ≥ 20 GeV²” acceptance is met **event-weighted** (+0.61 %) and **fails per cell** — 331 of the window's 1356 accepted cells (24.4 %) differ by > 1 %, worst ×6444 at x = 0.79, y = 0.0088, with 318 of them at y < 0.1; per cell it holds only for 0.15 ≤ y ≤ 0.7 (0.55 %), and it is 59 % at y = 0.985. The 2026-09-03 “0.16 % at y ≤ 0.7” claim was a four-point sample read as a window statement and is **withdrawn** (corrected 2026-09-04; T8(d)(i) now gates the low-y corner). **B3** the polarised quasi-elastic tail gets a price tag, not a calculation: `qe_tensor_scale` (default 0.0, shipped npz byte-identical), a **borrowed magnitude and not a derived bound**; measured Δ(ΔA_zz) 0.086 % / 0.0003 % / 0.25 % of the band at x = 0.01 / 0.10 / 0.30. **B4** the tagged `rc_tail ≡ 1` is half a kinematic fact — the elastic half is a veto, the quasi-elastic half is an omission of the same order as the inclusive quasi-elastic dilution; documented exclusion, no new term. **B5** the design record's `ℑ^el_6` had a spurious `η_A`; verified against `polrad2t.tex` and the FORTRAN, corrected in place with a dated box — and the CODE never carried it (Eq. (A.4) at `Q_N ≠ 0` is only on the unimplemented `PolradFull` path), so document-only, no number moved. **B6** the A = 2 → A = 6 transfer of `δ(x)` gets a price: `a_transfer_frac` (default 0.0, output byte-identical), `δ_eff = δ√(1+f²)`; band half-widths on A_zz at x = 0.01, Q² = 5 **1.358472e−04 / 1.518818e−04 / 1.921170e−04** at f = 0 / 0.5 / 1. The **Gakh–Shekhovtsova SHAPE is a recorded rejection**, measured from the paper's own arXiv EPS: δ ≡ (Δσ_RC − Δσ_Born)/Δσ_Born (inferred, corrected 2026-09-04) is singular across the b₁ zero-crossing (−1.691…+7.810 at Q² = 4), not monotone (−0.400 at x = 0.85 vs δ_high = 0.015), has no panel covering x = 0.01 Q² = 5, and is a ratio against *their* Born. `δ_low`'s source re-verified at **0 INSPIRE citations** (record 647050). **377 doctest cases / 17 216 856 assertions / 1 skipped, 268 pytest, 1071 refs 0 broken** (assertions re-measured 2026-09-04: the previously published 17 216 626 predated T8(d)(i)'s 230). | `phase_B_numbers.md` §§B1–B6 |
| C coherent ⁶Li + VMC (items 11, 1) | TODO | – | O1–O5, η, MC errors, the Mäntysaari ask |
| D sweep findings | TODO | – | ⁷Li tensor sector identically zero; SF injection; FSI variants; PomSet |
| E release hygiene | TODO | – | doc gate, metadata, CI/wheel (user's call) |
| F close-out | TODO | – | whole-diff review, doc refresh, author-decision batch |

States: TODO / IN PROGRESS (with what exists on disk) / DONE (with commit hash).

## Author decisions collected for the batch (Phase F)

Filled in as each phase produces the evidence; nothing here is decided silently.

| # | decision | phase | evidence |
|---|---|---|---|
| 1 | **`CdksB1` stops halving the digitized CDKS column** — `B1_CDKS_TABLE_TO_PER_NUCLEON` = 1, so `b1_convolution` / `CdksB1` / `--b1-model cdks` **doubled**. This is a *decision applied*, not merely recorded: the evidence is certain. | A (A3 → A4) | CDKS Eq. (10) carries an explicit 1/A, the text under their Eq. (16) says "b₁ is defined by the one per nucleon", f(y) is normalised to one nucleon, F₁ᴺ = (F₁ᵖ+F₁ⁿ)/2 — and the arbiter, HERMES's published b₁ᵈ, is per nucleon by their Eq. (5), confirmed by inverting their Table II in all six bins (mean ratio 0.946 against 0.473 for per-deuteron). `phase_A_miller_normalisation.md` §§2–3. Blast radius measured: two pinned values in `tests/test_sf.cpp` doubled; **no reference JSON moved** (the only b₁ reference is `"b1_model": "miller"`), so the rtol-1e−12 gate is untouched. |
| 2 | **Miller's ×0.5 STAYS** — `B1_MILLER_TABLE_TO_PER_NUCLEON` = `B1_PER_DEUTERON_TO_PER_NUCLEON` = 0.5. **Author decision: likely, not certain.** Nothing changed; the status quo is kept deliberately and the reasoning is now written down where the constant lives. | A (A3 → A4) | *For per deuteron (keep 0.5):* Eq. (1)'s densities are "in a target hadron", Eq. (5) is a light-cone correlator in the normalised deuteron state, Eq. (6)'s ½ is fully consumed by the quark-spin average of a spinless pion — no 1/A anywhere in Eqs. (1)/(5)/(6)/(20). *For per nucleon (drop it):* his Table I transcribes HERMES's per-nucleon values unrescaled, his Fig. 5 overlays them, and he tunes P₆q to one of them. **Numerical consequence of the other reading: every `MillerB1` number doubles**, including the DEFAULT inclusive tensor rate and `validation/reference/b1_default_li6.json` at rtol 1e−12. What would settle it: Miller himself, or evaluating his Eq. (20) at x = 0.012 (10.5×10⁻² ⇒ per nucleon; 5.25×10⁻² ⇒ per deuteron), which needs a pion PDF set this tree does not have. `phase_A_miller_normalisation.md` §§4, 7, 8. |
| 3 | **`Li6ConvolutionOptions` keeps `r_sigma_lt`** rather than adopting the gate's `r1998`. **Author decision.** Nothing changed. | A (A6) | The shipped observable is a ratio: the tensor weight is K/D_φ and D_φ's F₁ comes from `InclusiveKernel`'s own `UnpolSF`, whose R is `r_sigma_lt` because nothing sets it — a different R in the numerator would not cancel ((1+r1998)/(1+r_sigma_lt) is 1.088 at x = 0.1). **Measured cost of the choice** on x·b₁: −5.5 % / +24.6 % / +5.4 % / +2.7 % / −1.9 % at x = 0.05 / 0.10 / 0.20 / 0.30 / 0.50, all of it in the two orbital terms (term (1) is bit-identical — it carries an R-free b₁ᵈ table), where it is ×0.54 to ×0.94 because those terms convolve F₁ against a density integrating to zero and so see the *slope* of R. On the gate the same swap is +2.7 % on G3b (0.8432 → 0.8662), so the verdict does not rest on it. **The better third option, not taken here**: thread ONE R hook through `default_inclusive_kernel` into both objects. |
| 4 | **The ⁶Li publication ban is LIFTED**; the mandatory ±100 % band is NOT. | A (A7) | Design §5.4's *Escalation* clause reads the peak ratio **after** the checklist; after it the ratio is 0.843243 (AV18 + MSTW + Eq. (21)) and 1.000338 (CD-Bonn + MSTW + Eq. (21)), both inside the factor of 2. Conditions attached and repeated at every site: **the lift is about a configuration, not about a build** — the shipped default `ToyF2` gives **0.440**, outside the [0.5, 2] window, so numbers must be made with `--b1-unpol mstw` to be covered; it is marginal at Eq. (17) (0.520); the CD-Bonn 1.000338 is a residual below the reference's digitization error and not three-digit agreement with CDKS; and the gate is **A = 2** — it tests nothing about the α–d step, so the band, which comes from Q(⁶Li) vs Q_d, stays. `phase_A_numbers.md` §§0, 3, 8. |
| 6 | **`phase_C_numbers.md` §8.1c/§8.2's `A_zz(Born)` column is wrong by ×3.253983 and was NOT republished.** Recorded, not fixed: republishing that table is a task of its own and B3's brief did not cover it. | B (B3) | The column reproduces exactly as `azz(toy_b1(x, q2, f1, B1Mode::Digitized), …)` — the **deuteron** Miller b₁ table — while the shipped `default_inclusive_kernel` has used `Li6B1(MillerB1)` for every spin-1 ion since before the RC commit. Measured at Q² = 5: −1.473482e−03 / −4.975091e−03 / −1.585230e−04 (deuteron table, = the published column to five digits) against **−4.528242e−04 / −1.528923e−03 / −4.871661e−05** (this generator's ⁶Li) at x = 0.01 / 0.10 / 0.30. The `τ`, `w_hi − 1`, `ΔA_zz` and “band half-width 4.4204e−04” numbers inherit it; the `σ^el_T`, `σ^q_U` and `r_U` columns do **not** and reproduce here to every printed digit. `phase_B_numbers.md` §B3.2. |
| 7 | **The Gakh–Shekhovtsova δ(x) SHAPE is REJECTED, on the record** — the log-linear interpolation and its two anchors are kept. **Decision made, not an option overlooked**; the digitisation was deliberately not attempted. | B (B6) | Measured from the paper's own arXiv source EPS (`rrc01/rrc1/rrc4/rrc10.eps`, `Q² = 0.1/1/4/10`, each with `/x` plus RC and Born `/y` arrays): δ ≡ (Δσ_RC − Δσ_Born)/Δσ_Born (the RC as a fraction of the **Born**, the base the paper's own sentence names — inferred from its two curves, corrected 2026-09-04 from Δσ_Born/Δσ_RC − 1) is **singular** across the b₁ zero-crossing the RC itself moves (x₀ 0.20118 → 0.18467 at Q² = 4), spanning **−1.691 … +7.810** there and **−0.887 … +4.091** at Q² = 10, so the bounded ansatz `w = 1 ± δτ` would emit negative weights; it is **not monotone** (−0.400 at x = 0.85 against δ_high = 0.015 — 26.7× in magnitude, and T3 asserts monotone plus `==` on both anchors); its four x windows leave **x = 0.01, Q² = 5 uncovered** (and panels a/b do not even overlap); and it is a ratio against **their** Born (ALLM97 F₂, Callan–Gross b₂, their Eq. (60) A_zz), not this generator's b₁. By-product: the **magnitude** taken, "10–30 %", is the low-x end of the **Q² = 0.1 GeV²** panel alone — the only one inside the paper's own x ∼ 10⁻³–10⁻² — quoted at Q² = 5, now flagged. `phase_B_numbers.md` §B6.3, `design_C_tensor_rc.md` Q7. |
| 5 | **The `DeuteronConvolutionB1` wave-function default stays `kFdeutFile` (AV18)** even though CD-Bonn + MSTW reproduces CDKS's figure on every landmark. Nothing changed. | A (A5, restated here) | Flipping it would move every pinned gate number in layer 3 and checklist item 4 and every published row of `phase_A_numbers.md` §§0–7. It is a one-line change plus a re-pin and belongs to a task that says so. `phase_A_numbers.md` §8.6 item 1. |
